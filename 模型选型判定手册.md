# 模型选型判定手册：从"扫描数据"到"给建议"的可执行规则

> **这是什么**：一份把"什么时候该用什么模型"固化成规则的决策手册。它不是知识科普，而是一份**执行指令**——数据摆在你面前时，按本手册体检 → 查规则 → 给建议。
>
> **怎么用（对你——仓库主人）**：遇到新任务时，把**本文件 + 你的数据描述（或数据样例文件）**一起丢给任何 AI，说一句"请按《模型选型判定手册》给我模型选型建议"，它就会照 Part A 的指令执行。也可以你自己按 Part B 体检后直接查 Part C/D 的规则表。
>
> **规则来源**：本手册 v1.1 的规则提炼自**两个真实项目**：①充电站异常检测攻坚（excharge，PR-AUC 0.351→0.918，**分类谱系**）；②充电功率多步预测（voltaic，同源 Autosun 深圳数据、**回归/稀疏事件谱系**）。Part G 有完整工作示例（A/B 两个案例）。**数字均为真实值（excharge 取落盘 json；voltaic 取 docs/7模型对比报告.md），不虚构。**

---

## Part A｜直接复制给 AI 的调用指令

> 以下内容是一个**独立的指令块**。任何 AI（Claude / GPT / 文心 / DeepSeek / 豆包…）读到这里，都应当按它执行。把它连同数据一起粘贴给 AI 即可。

---

【系统指令：模型选型顾问】

你是一位模型选型顾问。用户给你一个数据任务，你的职责是：**先体检数据，再按《模型选型判定手册》Part B→C→D 的流程给出建议**，而不是凭直觉推荐热门模型。

执行步骤（必须严格按序）：

1. **任务定性（Part B0）**：先判断属于哪条谱系——分类/异常检测 → 走 R0-R9；回归/多步预测（尤其稀疏）→ 走 R0 + R10-R12。**定性错了，后面全错**（同源数据：excharge 异常检测与 voltaic 功率预测的规则完全不同）。
2. **体检数据（Part B）**：数据可读就用脚本实际扫描（附体检代码）；不可读或缺信息就**列出缺失维度并询问用户**，不要猜。
3. **判定其余维度**：逐项给出体检结论（D/L/N/I/P/X/C/E + 序列特性）。
4. **查规则表（Part C）**：按优先级从上到下匹配命中哪些规则（允许同时命中多条）。
5. **检查禁区（Part E）**：明确列出本数据下"绝对不能做的事"。
6. **给出建议**，输出格式必须为：

```
【数据体检结论】
| 维度 | 结论 | 证据 |
|------|------|------|
| 数据形态 | ... | ... |
| ... | ... | ... |

【首选方案】
- 模型：...（家族：...）
- 理由：命中规则 R...（解释，一句话）
- 前提假设：本方案成立需要 ...，请确认

【备选方案】
- ...

【本数据禁区】（命中 Part E 哪几条）

【第一步行动】（30 分钟内能做的具体事）

【风险提示】（什么情况下上述建议会失效）
```

7. 如果用户给的是**速查卡项目的数据**（excharge 异常检测 → Part H-1；voltaic 功率预测 → Part H-2），直接跳到对应速查卡，不要重新体检。

---

（指令块结束。以下为规则本体。）

---

## Part B0｜任务定性：先决定走哪条谱系（先于一切）

> 手册规则分两条谱系。**先定性，再体检，再查规则**——同源数据（excharge 与 voltaic 都是 Autosun 深圳充电站）换成不同任务，适用的规则天差地别。

| 谱系 | 任务形态 | 典型问题 | 主指标 | 核心规则 |
|------|---------|---------|--------|---------|
| **分类 / 异常检测** | 每个样本打标（正常 vs 故障/异常） | "这个样本/序列异常吗？" | PR-AUC / F1 | R0-R9 |
| **回归 / 多步预测** | 预测未来 N 步的数值曲线 | "未来 4 天充电功率多少？" | MAE/RMSE + 事件 F1 | R0 + R10-R12 |
| **混合（事件+量）** | 先判"有没有事件"，再估"多大" | "明天有没有充电高峰？功率多少？" | F1 + MAE_nz | R11 结构 + R6 |

---

## Part B｜数据体检清单：AI 必须扫描的 9 个维度

> 每个维度给出「怎么判断」和「为什么关键（真实教训）」。"怎么判断"里画 ✅ 的可以直接用体检代码自动得出，其余需问用户或看数据描述。

| # | 维度 | 取值选项 | 怎么判断 | 为什么关键 |
|---|------|---------|---------|-----------|
| D | **数据形态** | 纯表格 T / 纯序列 S / 表格+序列混合 TS / 其他（文本·图像·图） | ✅ 看结构：每行=独立样本→表格；每行=一条时间序列→序列；两者都有→混合 | 决定"哪个模型家族能上场"（Part C R1/R2/R3） |
| L | **标签** | 有全量标签 / 只有正样本标注 / 无标签 / 弱标签 | ✅ 看 label 列非空率 | 有标签→监督分类是主场；无标签却硬套重建式=自废武功（excharge 重建式 0.14 的教训） |
| N | **样本量与正样本率** | 样本量：<1k / 1k~50k / >50k；正样本率：**<5% / 5~20% / >20%** | ✅ 直接统计 | 决定：深度 vs 树；不平衡处理；是否需要 seed 集成（excharge 故障率 4.65% → 深度高方差是宿命） |
| I | **判别信息位置**（最关键） | 藏在统计特征里 / 藏在原始波形里 / 未知 | ⚠️ 实测法：先对"人工统计特征"跑一个 GBDT，再对"原始序列"跑一个简单序列模型，**谁的 baseline 高，信息就在谁那边** | excharge 胜负手：62 维统计特征几乎装下全部判别信息，纯深度吃原始信号只有 0.351；信息密度决定深度学习生死 |
| P | **训练/测试分布一致性** | 同分布 / 跨域（不同站点·设备·时间·人群） | ⚠️ 问用户数据怎么划分的；✅ 可做两域特征分布距离检验（KS / 均值差） | 跨域时端到端学到的"逐点时序模式"不迁移，只有物理/统计规律跨域成立（owner1-6→7-8 的教训） |
| X | **可解释性要求** | 无要求 / 需要局部归因（为什么这个样本异常）/ 需要机制级归因（异常发生在前段还是末段、由哪些特征驱动） | ⚠️ 问用户（论文/合规/客户需要） | GBDT 再强也是黑盒，可解释性刚需时它当不了主模型（excharge TAIG/EU AI Act 卖点） |
| C | **算力** | CPU only / 单块消费级 GPU / 服务器级 GPU（A100/H100 等） | ⚠️ 问用户 | 决定"探路→量产"两阶段策略（Part F）；探路用小模型，量产才上大模型+多 seed |
| S | **序列特性**（仅当 D=序列/混合） | 序列长度分布（是否变长）/ 通道数 / 是否有 padding | ✅ 统计长度分布；⚠️ 问通道含义 | 变长+padding 会稀释信号；多通道原始信号直接喂 RNN 通常是信息密度灾难 |
| E | **环境/口径一致性** | 特征版本是否统一 / 训练环境是否统一 / **评估协议是否有泄漏** | ⚠️ 问用户有没有多个特征版本、是否换过机器；✅ 见 E11 核验法 | 不同特征口径的数字不可比（57 维 0.893 ≠ 62 维 0.887）；Mac MPS 与 H100 CUDA 的 backend 差异会导致 XGBoost 0.861 vs 0.887；评估泄漏会把假成绩当真（voltaic TimesNet） |

**体检代码（可直接跑的通用脚本）**——把数据路径换成你的，AI 或你本人都可以运行：

```python
import pandas as pd, numpy as np
# 按实际格式读：parquet / csv / excel
df = pd.read_parquet("你的数据.parquet")   # 或 read_csv / read_excel
print("形状:", df.shape)
print("列类型分布:\n", df.dtypes.value_counts())
# 找疑似标签列（y/label/target/anomaly/故障...）
lab = [c for c in df.columns if str(c).lower() in
       ("y","label","target","anomaly","fault","故障","异常")]
if lab:
    y = df[lab[0]]
    print(f"标签列 {lab[0]}: 正样本率 = {y.mean():.4f} (正样本 {int(y.sum())} 条)")
# 数值 vs 缺失
print("缺失率最高 5 列:\n", df.isnull().mean().sort_values(ascending=False).head(5))
print("数值列 describe:\n", df.select_dtypes("number").describe().T[["mean","std","min","max"]].head(10))
# 如果含序列（id 列 + 时间戳列），打印序列长度分布
idc = [c for c in df.columns if "id" in str(c).lower() or "seq" in str(c).lower()]
if idc:
    print(f"疑似序列 id 列: {idc}; 分组条数分布:")
    print(df.groupby(idc[0]).size().describe() if idc else "N/A")
# 若样本本身是矩阵/数组（如 .npy 序列集），单独打印 shape
# arr = np.load("seq_X.npy"); print("序列张量:", arr.shape, "值域", arr.min(), arr.max())
# 若任务是预测目标数值曲线（回归/多步预测），打印目标稀疏度
# tgt = df["power_kw"]  # 举例
# print("目标零值占比: %.1f%%" % ((tgt == 0).mean() * 100))
```

---

## Part C｜判定规则总表（核心）

> **使用方式**：先按 Part B0 定谱系。分类谱系查下方 R0-R9；预测谱系查 R0 + 第二谱系 R10-R12。允许同时命中多条。
> **一句话哲学**：先确定"任务定性 + 判别信息在哪 + 前提假设成不成立"，再谈模型；模型永远服务于信息入口。

### 第一谱系 · 分类 / 异常检测（R0-R9）

| 规则 | 触发条件 | 建议动作 | 真实佐证 |
|------|---------|---------|---------|
| **R0 免费天花板** | **任何**表格型/可特征化的任务，动手第一步 | 先对特征口径跑 **LightGBM**（表格）/ 或先做切段统计特征再跑 GBDT（序列）。1 秒出结果，它告诉你"这活儿天花板多高"，给所有深度模型设靶 | excharge LightGBM 0.868 = 全项目参照系；深度打平它即算成功 |
| **R1 信息在特征里** | I=统计特征强（GBDT 明显 > 原始序列模型），且任务要求深度/端到端 | **特征必须进网络**（把特征当 token/输入），禁止让模型饿着肚子从原始信号重新提取已被蒸馏的信息；可上 FT-Transformer 式特征 tokenizer | excharge 纯 Bi-LSTM 吃 6 通道原始曲线 = 0.351；同样的信息装进 62 维特征后拼接融合 = 0.829 |
| **R2 信息在波形里** | I=原始波形强（特征工程榨不出 > GBDT 基线） | 上**监督序列模型**（LSTM/GRU/1D-CNN/Transformer/PatchTST）；样本 >5k 可先自监督预训练再微调；序列很长先切段/Patch 化 | 反向案例：iTransformer/PatchTST/FT-Transformer 在"信息其实在特征里"的任务上全灭（0.592/0.744/0.716）——不是它们差，是信息入口错 |
| **R3 序列×特征混合** | D=表格+序列混合 **且两者都有判别信号** | **首选"同空间交互融合"**：把序列切成段、每段统计量做 token，与全局特征 token 一起进同一注意力空间互查（Token-Attn / Cross-Attn 结构）；**逐种交互方式实测**，禁止默认拼接/门控会赢 | excharge：Token-Attn 0.851（单 seed 系）与 Cross-Attn 0.845 有效；**简单拼接早期 0.829、门控 Gated 0.796 反而退步**——"怎么交互"比"加不加注意力"重要 |
| **R4 无标签想做异常检测** | L=无标签/弱标签 | **先判断异常结构**：①点异常/数值越界→统计方法（3σ/IQR）+ Isolation Forest，深度非主场；②结构性异常（整段模式突变）且正常样本海量→重建式（AE/LSTM-AE/USAD），但**必须先验证"正常可重建、异常重建差"**（拿少量标注算两类重建误差分布），验证不过别用 | excharge 重建式 LSTM-AE = **0.140**：异常是"微弱偏移"型，AE 能把它也平滑重建，"重建得好不好"根本不区分正常/异常 |
| **R5 跨域/分布漂移** | P=训练域 ≠ 测试域 | ①**量化漂移**（两域特征分布距离），小漂移正常训；②漂移大时：优先用**跨域成立的物理/统计规律特征**（而非逐点时序模式）；③目标域有少量样本→few-shot 微调；④域自适应（CORAL/DANN）**只在"两域标签函数一致、仅输入分布漂移"时**才值得试；⑤评测必须**分域切分**（train 域训、test 域测），严禁混洗 | excharge CORAL 0.825 ≈ 基线 0.825，**收益≈0**：新站点故障签名本身不同，对齐输入分布没用 |
| **R6 强不平衡** | 正样本率 <10% | ①**主指标用 PR-AUC**（不是 accuracy/AUC）；②深度模型在强不平衡下**高方差是宿命**→必须多 seed + 集成 + 统计检验；③Focal Loss / 重采样 / 条件增强**不默认有效，加对照实测**；④树模型记得开 `scale_pos_weight` | excharge 故障率 4.65%：单 seed 深度 0.819~0.921 波动极大，7-seed 集成才稳定到 **0.918**；3 条数据增强路线全部负结果（0.815~0.867）——信号已被特征榨干，增强补不进来 |
| **R7 小样本** | 样本量 <1k（或正样本 <200） | GBDT 是主场；深度只配"小而强+强正则+多 seed"；可用 TabPFN 做**表格式小样本探针**（实验性）；不要上大 Transformer | （excharge 正样本 642/测试 129 属边缘区，深度靠 7-seed 集成救回；更小就别赌深度） |
| **R8 可解释性刚需** | X=需要归因（论文/合规/客户） | 表格→XGBoost+SHAP；序列×特征→**自注意力归因**（特征 token 的注意力分布即证据，Token-Attn 结构白送可解释性）；明确"输出+归因都要"，别选解释不了的黑盒 | excharge 靠注意力图把"异常=前段特征塌缩/高功率未衰减"讲成机制级卖点，GBDT 无法提供 |
| **R9 算力受限** | C=本地消费级/笔记本 | 两阶段：**本地小模型 1-seed 探路**（看相对 GBDT 有没有增益）→ 有效方向才上服务器**量产多 seed/大模型**；探路看方向、量产看绝对分 | Mac MPS 单 seed 50~68min/次，一晚串行探 3 个方向；有效变体（Token-Attn）才上 H100 量产 |

### 第二谱系 · 回归/多步预测（由 voltaic 项目补充）

> 适用：Part B0 定性为预测未来数值曲线（功率/负荷/销量/流量等），尤其**稀疏零膨胀**（大量时刻值=0）。真实佐证全部来自 voltaic（28 case 充电功率，预测 gt 4 天 = 384 步，真 out-of-sample，与 excharge 同源 Autosun 数据但任务不同）。

| 规则 | 触发条件 | 建议动作 | 真实佐证 |
|------|---------|---------|---------|
| **R10 朴素基线三件套先行** | **任何**多步外推/预测任务 | 深度模型开训前必须全跑：①**All-Zero / 中位数填充**——假阳性哨兵，模型若比它差 = 在纯外推假阳性；②**同时段历史中位数**（按工作日 × 15min 时段取历史中位）——捕捉季节先验，常常就是真正的天花板；③**GBDT 特征版**（lag / rolling / 时间循环编码）。三者全赢过，深度才配继续 | voltaic：**Slot-Median MAE 27.68 全场最优**；PatchTST 33.74 / TimesNet 37.08 **不如 All-Zero 31.54**——稀疏场景深度外推 = 假阳性机器 |
| **R11 稀疏/零膨胀 → 两阶段门控结构** | 目标值大量=0（稀疏），既要判事件又要估量 | 别让单模型直接回归——会被"平均化"毒害（学个中间值，既漏事件又报假阳性）。用**两阶段 / Hurdle 门控**：阶段 1 分类"有没有事件"，阶段 2 只在非零样本上回归"多大"；**加业务域计数特征**（活跃枪数 / 在用设备数 / 在场车辆数）防止阶段 1 塌缩（全判零） | voltaic：我方两阶段 LightGBM + 活跃枪数特征把事件 F1 从 **0 → 0.696**；对照：教授 Hurdle 缺该域信号，在其数据上 **F1 = 0**——阶段 1 全判零 = 两阶段塌缩 |
| **R12 指标跟着业务目标走** | 零膨胀 / 多目标评估 | 点误差看 MAE/RMSE；**事件检测单独用 F1**，非零步用 MAPE_nz；警惕 **WAPE 失真**——真值总和≈0（gt 落在低谷）时 WAPE 爆表（15~29）不代表模型差，先检查评估集是否含"真值几乎全零"段，有则剔出或分段统计 | voltaic：各模型 WAPE 15~29 远超理论值 1，根因是部分 case 的 gt 4 天恰好全零；且**点预测赢家（Slot-Median）与事件检测赢家（DEMMFL，F1 0.715）不是同一个模型**——先定业务目标再选赢家 |

**跨谱系通用提醒**：
- 深度外推模型比 All-Zero 还差（TimesNet 37.08 vs 31.54）不是 bug，是**该场景深度模型的真实水平**——诚实报告它，也别因此否定所有深度模型（换任务/换结构可能翻盘，见 excharge 的 R3）。
- 预测任务的"按 case 切分、各训各测"本身就是真 out-of-sample（对应 R5 精神）；若再用历史窗口训练并滚动评测，即命中 E11。

---

## Part D｜决策树（主流程一图流）

```
拿到数据
 ├─► 任务定性（Part B0）
 │     ├─ 回归/多步预测（功率/负荷/销量…）──► 预测谱系：
 │     │     R10 朴素三基线：All-Zero(假阳性哨兵) / 同时段中位数(季节先验) / GBDT 特征版
 │     │     ├─ 目标稀疏（大量=0）──► R11 两阶段门控结构 + 业务计数特征
 │     │     └─ 密集连续 ──► 赢过三基线的特征 GBDT / 深度预测模型
 │     │     指标：MAE/RMSE + 事件 F1；警惕 WAPE 失真（R12）
 │     └─ 分类/异常检测 ──► 走下方分类谱系
 │
 ├─ 数据是表格/可特征化？──否──► 文本→LLM；图像→CNN/ViT；图→GNN（超出本手册范围）
 ├─ 是
 ├─► R0：先跑 LightGBM（表格）或先切段统计→GBDT（序列）  ←——永远的第一步
 │
 ├─► 体检：I 判别信息在哪？ L 有标签吗？ P 跨域吗？ X 要解释吗？ N 不平衡吗？
 │
 ├─ 无标签 ──► R4：判断异常结构 → 点异常走统计 / 结构性异常才考虑重建式(先验证前提)
 │
 ├─ 有标签 ──► 判别信息在？
 │     ├─ 统计特征强（GBDT 高）──► 需要深度故事吗？
 │     │     ├─ 否 ──► 就 GBDT + SHAP，收工（别为了深度而深度）
 │     │     └─ 是 ──► R1：特征进网络（FT-Transformer 式 / 特征 token），别让模型饿着吃原始信号
 │     ├─ 原始波形强 ──► R2：监督序列模型 +（样本够）自监督预训练
 │     └─ 混合都有信号 ──► R3：同空间交互融合（Token-Attn / Cross-Attn），逐个实测交互方式
 │
 ├─► 无论上面走到哪，叠加检查：
 │     P 跨域 ──► R5（物理/统计特征优先；域自适应仅在前提成立时试）
 │     N<10% ──► R6（PR-AUC 主指标；多 seed 集成 + 统计检验；增强要对照）
 │     N 很小 ──► R7（GBDT 主场；TabPFN 探针）
 │     X 要解释 ──► R8（注意力归因 / SHAP）
 │     C 受限 ──► R9（本地探路 → 服务器量产）
 │
 └─► 禁区检查（分类谱系 E1-E10；预测谱系加 E11-E12）→ 输出 Part A 模板的建议
```

---

## Part E｜禁区清单：什么情况"绝对不要"用什么（两个项目的血泪）

> 这些不是"不推荐"，是"两个项目（excharge 异常检测 + voltaic 功率预测）已经替你撞过墙"的明确禁止项。E1-E10 来自 excharge，E11-E12 由 voltaic 补充。

| # | 禁区 | 原因（真实数字） |
|---|------|-----------------|
| E1 | **有标签却用重建式/无监督当主力** | 监督信息白白扔掉；重建式 0.140，监督路线随便 0.8+ |
| E2 | **端到端深度直接消化低信噪比原始多通道信号**（尤其变长序列） | 判别信号被逐点噪声稀释：纯 Bi-LSTM 0.351；iTransformer/PatchTST/FT-Transformer 全灭（0.592/0.744/0.716） |
| E3 | **默认拼接/门控/加个注意力就会涨** | excharge 三个融合变体三种结局：Token-Attn 涨、简单拼接早期平庸、Gated **退步**（0.796）；"交互方式"必须逐个实测 |
| E4 | **跨域任务里让 BatchNorm/归一化偷看测试域分布** | 训练时对测试域前向 → BN 提前学到测试分布 → 0.81 假象；修复（eval 包裹前向）后打回原形 |
| E5 | **单 seed 报深度模型成绩** | 强不平衡下深度单 seed 方差巨大（0.819~0.921），单跑一个数字等于掷骰子 |
| E6 | **不平衡任务用 accuracy 当主指标** | 99% 正常的模型 accuracy 也 95%+，毫无意义；用 PR-AUC |
| E7 | **拿不同特征口径/不同环境的数字直接比较** | 57 维 XGBoost 0.893 ≠ 62 维 0.887；Mac MPS 的 0.861 ≠ H100 CUDA 的 0.887（backend 差异）——比数字前先对齐口径 |
| E8 | **套文献"政治正确"范式而不检查前提** | 重建式要"异常结构性强"，域自适应要"标签函数跨域一致"——两前提都不成立时硬套 = 白费数天 |
| E9 | **把"换模型"理解成"扔特征"** | 导师/客户说"要深度模型"时，正确翻译是"让特征以深度方式参与"，不是"抛弃特征从头学"——0.893→0.351 暴跌的根源 |
| E10 | **声称增强/正则有效但没做对照** | Focal Loss 在 excharge 卡死、3 条增强全负结果；任何"加了就涨"的宣称都要 paired 对照实验背书 |
| E11 | **训练窗口采样覆盖了测试真值（评估协议泄漏）** | voltaic 曾用 rolling-origin + 整序列 linspace 采样训练窗口，训练样本盖到测试段 384 步 → TimesNet 假成绩 **27.23（"全场最优"）** → 修掉泄漏后 **37.08（不如 All-Zero 31.54，全场最差）**。核验法：**训练样本时间范围 ∩ 测试真值时间范围必须为空**；测试集一旦切出，训练脚本任何一步都不得再读它 |
| E12 | **稀疏外推不先跑朴素基线就上深度模型** | 深度模型依赖近期窗口外推，gt 恰好稀疏时会狂报假阳性：voltaic 的 PatchTST 33.74 / TimesNet 37.08 **全部差于 All-Zero 31.54**——没有 All-Zero/中位数打底，你分不清"模型真会预测"还是"模型在瞎猜" |

---

## Part F｜配套纪律：让上面的建议真正落地

1. **永远先立免费天花板**（R0）：LightGBM 1 秒出分，没有它你无法判断深度模型是"赢了"还是"只是没输得那么惨"。
2. **深度模型成绩 = 多 seed 集成 + 统计检验**：seed ≥5（excharge 用 7），概率平均（不是投票），再配 bootstrap / DeLong 检验说"显著优于 X"而不是"高于 X"。+0.045 就是集成白捡的。
3. **断点续训必须存全**：只存权重不存优化器状态 → 训练中断再续 = 假成绩（excharge 0.748 假象的真凶是没存 AdamW 状态）。要么完整训练，要么 checkpoint 存 `model + optimizer + epoch + seed`。
4. **每次实验落盘元数据**：seed / 环境（MPS or CUDA）/ 特征版本 / 数据切分 / 耗时 / 评估协议。没有元数据的分数不可信、不可比。
5. **负结果也是结果**：记录"什么没涨"（增强无增益、域自适应≈0、深度不如全零），它防止你下次重走弯路，也是论文诚实性的加分项。
6. **分两阶段花算力**：探路（本地、小模型、1-seed、看方向）→ 量产（服务器、大/多模型、7-seed、看绝对分）。探路阶段任何变体赢不了 GBDT 就砍。
7. **评估协议先自证无泄漏**（E11）：任何"训练窗口从序列采样"的流程，先打印**训练样本时间范围 ∩ 测试真值范围**，交集为空才允许开跑；"测试集一旦切出就再不被训练脚本读取"应作为硬性代码规范（voltaic 的 TimesNet 假最优 27.23 就是漏了这一条）。
8. **预测任务先立朴素基线三件套**（R10）：All-Zero（假阳性哨兵）+ 同时段中位数（季节先验）+ GBDT 特征版。深度外推模型只有全赢过它们才有存在意义（voltaic：中位数 27.68 全场最优，两个深度模型反而比 All-Zero 差）。

---

## Part G-1｜工作示例 A：充电站异常检测（excharge，分类谱系）

> 用本项目真实数据演示"手册怎么用"。对照案例 B（voltaic 预测谱系）见 Part G-2——**同源 Autosun 数据，任务从"判异常"换成"预测功率"，规则怎么跟着变**，对比看最涨功力。

**场景**：深圳 30 个充电站、19,658 条充电序列、155.6 万采样点。目标：检测充电故障（安全相关）。要求：跨站泛化（owner1-6 训练 → owner7-8 测试）、端到端深度、可解释（EU AI Act）。

**Step 1 体检**：D=表格(62 维统计特征)+序列(6 通道原始曲线)混合；L=有标签（642 条故障，序列级故障率 4.65%）；N=19,658 条、正样本 642；I=未知（当时没测！）；P=跨域（不同 owner 站点）；X=机制级归因刚需；C=先是 Mac MPS、后 H100。

**Step 2 按规则走**：
- R0 免费天花板：LightGBM 62 维 = **0.868**（1 秒）。——天花板立起来了。
- **当时漏掉的关键一步**：没做 I 的实测（特征 GBDT vs 原始序列模型谁高）。如果做了，就会立刻发现特征口径 0.868 ≫ 原始口径 0.351，直接命中 R1，后面纯端到端的坑全可以绕开。
- R1 实际发生的教训：导师要求端到端深度 → 被误翻成 E9"扔特征吃原始信号" → Bi-LSTM 0.351。正确翻译应是"62 维特征进网络"。
- R3（最终走通）：混合数据、且特征与序列段都有信号 → Token-Attn 把序列段 token + 特征 token 放同一注意力空间 → 单 seed 0.851 → H100 量产。
- R6（叠加命中）：不平衡 4.65% → PR-AUC 主指标 + 7-seed 概率集成 → **0.918**，bootstrap p=0.026 边际显著超 LightGBM。
- R8（叠加命中）：可解释刚需 → 注意力图给出机制级叙事（异常 = 高功率未衰减/前段特征塌缩型）→ 成为论文卖点。

**Step 3 禁区命中自查**：E1（曾想重建式，实测 0.140 果断弃）✅ 验证了规则；E2（四个编码器全灭）✅；E3（Gated 0.796 退步）✅；E4（BN 泄露假象）✅；E5（单 seed 方差）✅；E7（57/62 维口径、Mac/H100 环境差异）✅。

**Step 4 结论复盘**：最终赢的不是"更强的模型"，而是 **R1+R3 的信息入口修正（特征进注意力空间）** + **R6 的报告纪律（集成+检验）**。前者把模型从"错误入口"拉回"正确入口"，后者把"有希望"变成"可信的赢"。

---

## Part G-2｜工作示例 B：voltaic 稀疏功率预测（对照：任务变了，规则怎么变）

> 与案例 A **同源数据**（深圳 Autosun 充电站），但任务从"异常检测分类"换成"功率多步预测"——对照最能说明手册价值：**不是模型变强了，是规则换对了**。

**场景**：28 个 case（站点），15min 网格级功率序列 + 活跃枪数序列，预测每 case 未来 4 天（384 步）功率曲线；训练=data、测试=gt（真 out-of-sample）。稀疏：充电低谷大量时刻功率=0。

**Step 1 定性 + 体检**：B0 → **预测谱系**（不是分类！）；D=单变量功率序列（+业务计数序列活跃枪数）；L=无人工标签（监督预测自身历史）；N=28 case × 数百天；I=季节性 + 近期状态混合；P=各 case 独立、真 OOS；X=无强制；C=CPU/本地。

**Step 2 按规则走（用 v1.1 新规则回看当时的对错）**：
- R10 朴素三基线：当时 All-Zero 与 Slot-Median 都跑了——✅ 做对了。**Slot-Median MAE 27.68 全场最优**，说明该任务"季节先验 >> 模型复杂度"。
- **当时踩的雷（新规则事后能拦住）**：①深度模型 PatchTST/TimesNet 在没赢过 All-Zero 的情况下仍被当作主力深度调参——命中 **E12**；②rolling-origin + 整序列采训练窗口的泄漏让 TimesNet 一度报 27.23"全场最优"，修复后跌到 37.08（全场最差）——命中 **E11**，假象推翻比性能差更致命。
- R11 稀疏结构：我方**两阶段 LightGBM + 活跃枪数特征**解决阶段 1 塌缩，事件 F1 **0 → 0.696**——"结构 + 域信号 > 模型大小"。
- R12 指标：报告用 MAE+WAPE+F1 多口径，正确识别出 WAPE 15~29 是"gt 全零段"评估失真而非模型差——✅ 做对了。

**Step 3 结论复盘**：voltaic 的输赢结构与 excharge **同构**——朴素基线/特征碾压复杂模型、假成绩来自协议漏洞、救场的是"把业务信号正确接进结构"。两个项目合起来才是完整方法论：**分类谱系看"判别信息位置"，预测谱系看"朴素基线与稀疏结构"，两者共同铁律是"先验基线、无泄漏评估、诚实报告"**。

---

## Part H-1｜速查卡一：excharge（充电站异常检测）

> 如果你再次拿到的是本项目的延续数据（excharge 仓库），**不必重新体检**，直接按此卡执行。若任务描述与卡不符，才回到 Part B 重新体检。

| 项 | 速查值 |
|----|--------|
| 数据形态 | 62 维统计特征表格 + 6 通道原始充电曲线（变长 30~200 步，需 padding） |
| 标签 | 有监督：642 条序列级故障，正样本率 4.65%（强不平衡） |
| 数据切分 | **跨站**：owner1-6 训练 → owner7-8 测试（严禁混洗） |
| 主指标 | **PR-AUC**（报告同时给 AUC/F1） |
| 参照基线 | LightGBM 62 维 **0.868**（Mac/H100 一致）；XGBoost H100 0.887（注意环境口径） |
| 已验证负结果（勿重试） | 重建式 LSTM-AE 0.140；CORAL 域自适应收益≈0；iTransformer/PatchTST/FT-Transformer/Dual-Transformer（0.592/0.744/0.716/0.275）；多尺度池化 0.806；Gated 0.796；数据增强 3 路（0.815~0.867）；Focal Loss（卡死） |
| 当前最优 | **Token-Attn（特征 token + 序列段 token 同空间交互）+ 7-seed 概率集成 = PR-AUC 0.918 / AUC 0.9903 / F1 0.795** |
| 可解释性 | 自注意力图：异常时特征 token 关注上升、启动段(2/8)注意力塌缩 = "前段特征塌缩/高功率未衰减"型故障；电气中断型 95.3% / 超温型 4.7% |
| 训练环境 | H100 CUDA；Mac MPS 仅可探路（单 seed 50~68min，禁止并行训练会死锁） |
| 主框架 | `train_c1c2.py`（环境变量 ONLY/BATCH/EPOCHS/SEED/DEVICE 控制） |
| 下一步合理方向 | 特征组消融（寻找最小特征集）、跨站 few-shot（拿 owner7-8 少量标注）、机制型规则与注意力对照、把 Token-Attn 结构迁移到其他"表格×序列"任务验证通用性 |

---

## Part H-2｜速查卡二：voltaic（充电功率预测·稀疏零膨胀）

> 数据在 `/Users/arthas/git/voltaic`（与 excharge 同源 Autosun 深圳数据，但任务是预测不是检测）。若用户给的是该仓库数据，直接按此卡执行。

| 项 | 速查值 |
|----|--------|
| 数据 | 28 case（站点），15min 网格级功率 kW 序列 + **活跃枪数**序列（CSV 28 字段，Autosun 深圳） |
| 任务 | **多步预测**：每 case 预测未来 2h/4h/1d/4d 功率曲线（主口径 4d = 384 步）；训练=data、测试=gt，**真 out-of-sample**，无泄漏 |
| 稀疏程度 | 充电低谷大量时刻功率=0（ACT_THR=0.05kW 判活跃）→ 零膨胀回归 + 事件检测混合任务 |
| 指标 | MAE/RMSE（点误差）+ **F1（事件检测）** + MAPE_nz；⚠️ WAPE 在 gt 全零段会爆表失真（各模型实测 15~29） |
| 当前最优 | 点预测：**Slot-Median（同时段历史中位数）MAE 27.68 全场最优**；事件 F1：DEMMFL 0.715 / GRU 0.715；我方两阶段 LightGBM+活跃枪数 MAE 31.69 / F1 0.696 |
| 朴素基线（必跑） | **All-Zero MAE 31.54 = 假阳性哨兵**；PatchTST 33.74 / TimesNet 37.08 **均差于 All-Zero**——勿再让深度模型直接裸跑稀疏外推（E12） |
| 关键历史教训 | **rolling-origin 泄漏**：训练窗口曾覆盖测试 384 步 → TimesNet 假报 27.23"最优"→ 修复后 37.08 最差（E11）；两阶段塌缩：无域信号时 Hurdle F1=0，加活跃枪数 → 0.696 |
| 代码 | `voltaic/code/`：`model_zoo.py`（我方：LightGBM-TwoStage/PatchTST/TimesNet）、`professor_model_zoo.py`（教授：Slot-Median/DEMMFL/GRU）、`run_merged_benchmark.py`（7 模型统一评测） |
| 口径备注 | 数字以 `docs/7模型对比报告.md` 为准（predictions/merged_benchmark.json 个别数字略异，疑版本不同）；**点预测赢家 ≠ 事件检测赢家** → 先定业务目标再选模型（R12） |

---

*手册版本 v1.1（2026-09-03）。v1.0 提炼自 excharge（分类谱系）；v1.1 由 voltaic（预测谱系）补充 R10-R12、E11-E12、B0 任务定性、双速查卡。规则若与现实冲突，以"任务定性 + 判别信息位置 + 前提假设"三条铁律为准重新推导。*
