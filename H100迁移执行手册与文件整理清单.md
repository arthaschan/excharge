# H100 迁移执行手册 + 项目文件整理清单

> 日期：2026-09-02 ｜ 目的：excharge 训练从 MacBook（MPS，内存受限）迁移到 H100（CUDA），含同步清单、环境搭建、代码适配、冒烟测试与本地整理建议。
>
> 背景：Mac MPS 上 30 epoch 单变体约 30~68 分钟、BATCH 被压到 64、focal 还出过 swap 死锁；H100 上同一框架可全速跑，多 seed/预训练/生成式增强才有可行性。

---

## 一、项目文件盘点（分类清单）

总大小约 **1.2 GB**（不含 .git 401MB 与 .DS_Store 等）。已按"H100 是否必需"分类：

### A 类：H100 训练必需（约 112 MB，必须同步）
| 路径 | 大小 | 说明 |
|------|------|------|
| `scripts/` | 404 KB | 全部训练/评估/出图脚本（train_c1c2.py 为主框架） |
| `data/real/fusion_data.pkl` | 39 MB | **训练主数据**（X_tensor 6通道序列 / X_feat 62维 / feat_cols / split 划分） |
| `data/real/all_data.parquet` | 33 MB | 全量 155.6 万点原始（异常率/owner 重算用） |
| `data/real/seq_tensors.pkl` | 34 MB | 序列张量缓存（biLSTM/attn 脚本用） |
| `data/real/fusion_owner.pkl` | 173 KB | owner 标签（域自适应/跨站分析用） |
| `data/real/seq_*.parquet` + `seq_features*.json` | ~7 MB | 旧管线的序列级拆分（v1/v2 两代，保留不动） |

### B 类：对照/可选（约 20 MB 以内，建议少量同步或不同步）
| 路径 | 说明 |
|------|------|
| `docs/c1c2_*_results.json` + `*_prob.npy`（共 ~10 个） | 本地基线结果，**建议同步**（服务器上新实验直接对比口径） |
| `docs/c1c2_tokenattn_s123_model.pt`（915 KB） | seed123 主模型权重（如需在服务器做推理/出图） |
| 根目录 4 个分析文档（.md） | 参考用，可不同步（本地看即可） |
| `docs/tokenattn_attn_figs/` | 4 张注意力图，不同步（已交付） |

### C 类：无需同步（约 1 GB）
| 路径 | 大小 | 说明 |
|------|------|------|
| `.git/` | 401 MB | 只在本地做版本管理；H100 上不需要历史 |
| `dataset /`（processed_data.xlsx 111M + _longer_than_30 98M） | 209 MB | **原始 xlsx**。标准迁移不需要；仅当要在 H100 上重跑特征管线（preprocess.py→build_*）才同步 |
| `A dataset of EV batery...zip` | 197 MB | 原始 zip，与 `dataset /` 解压内容重复 |
| `归档.zip` | 62 MB | 8 月底旧归档，见第四节 |
| `references/`（文献 PDF 59M）、`paper/`（5.7M）、`update/` `update2/`（docx/pdf） | ~66 MB | 写作材料，服务器不需要 |
| `docs/` 下 ckpt/npy/图等全部历史产物 | ~10 MB | 本地留档 |

> 结论：**标准迁移只需 ~112 MB（A 类）+ 少量 B 类 json**，非常轻。

---

## 二、同步命令（rsync）

### 2.1 标准迁移（推荐，~120 MB）
```bash
# 在 Mac 本地执行；把 HOST/路径换成你的 H100 实际地址
HOST=user@h100-ip   # 例如 root@x.x.x.x 或经 VSCode Tunnel 的跳板机
REMOTE=/root/excharge

rsync -avP --exclude '.git' \
  --exclude 'dataset ' --exclude '*.zip' --exclude 'references' \
  --exclude 'paper' --exclude 'update*' \
  --exclude 'docs/c1c2_*_model.pt' --exclude 'docs/*_attn_figs' \
  /Users/arthas/git/excharge/scripts/   "$HOST:$REMOTE/scripts/"
rsync -avP /Users/arthas/git/excharge/data/real/ "$HOST:$REMOTE/data/real/"
# 本地基线结果（口径对照）
rsync -avP /Users/arthas/git/excharge/docs/c1c2_*_results.json \
           /Users/arthas/git/excharge/docs/c1c2_*_prob.npy "$HOST:$REMOTE/docs/"
```

### 2.2 完整可复现迁移（含原始 xlsx，~330 MB）
```bash
# 仅当要在 H100 上重跑 build_fusion_data.py / preprocess.py 时才需要
rsync -avP "/Users/arthas/git/excharge/dataset /" "$HOST:$REMOTE/dataset/"
```

### 2.3 校验
```bash
ssh "$HOST" "cd $REMOTE && du -sh scripts data/real && ls scripts/ | head"
# 本机对照：du -sh data/real 应一致（111M）
```

---

## 三、H100 环境搭建

### 3.1 一次性依赖（requirements 文件已生成：`requirements_h100.txt`）
```bash
conda create -n excharge python=3.10 -y && conda activate excharge
pip install -r $REMOTE/requirements_h100.txt
# torch 用 CUDA 版（H100 = sm_90，装 cu124/cu128 均可）
pip install torch --index-url https://download.pytorch.org/whl/cu124
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# 期望: True NVIDIA H100 ...
```
按需追加（跑对应实验才装）：
```bash
pip install tabpfn        # P0d 旁证
pip install ruptures catch22   # P0c 特征验证
pip install tsaug         # P1a 数据增强（tsaug 老，若冲突改用自写增强，见方案文档）
git clone https://github.com/yuezhihan/ts2vec.git && pip install -e ts2vec   # P2a 预训练
```

### 3.2 代码适配点（已在本地核对，改动极小）
| 文件 | 现状 | 处理 |
|------|------|------|
| `scripts/train_c1c2.py`（主框架） | 第 55-56 行支持 `DEVICE` 环境变量（默认 mps→cpu） | **零改动**，跑时 `DEVICE=cuda` 即可 |
| `scripts/make_tokenattn_attn_figures.py`（出图） | 第 32 行硬编码 `'mps' if torch.backends.mps.is_available()` | 小补丁（见下） |
| `train_fusion.py` / `train_seq_bilstm_v2.py` / `explain_fusion_sensitivity.py` 等 | 旧管线，mps 硬编码 | **不迁移**（fusion 旧管线 = 断点续训产物，已被 train_c1c2 取代） |

出图脚本补丁（在 H100 上执行前改一行）：
```bash
# 把 make_tokenattn_attn_figures.py 第 32 行替换为：
#   device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
sed -i '' "s/torch.device('mps' if torch.backends.mps.is_available() else 'cpu')/torch.device('cuda' if torch.cuda.is_available() else 'cpu')/" scripts/make_tokenattn_attn_figures.py
```

### 3.3 训练策略升级（H100 vs MPS）
| 项 | Mac MPS（旧） | H100（新） | 说明 |
|----|-------------|-----------|------|
| BATCH | 64（内存紧） | **256**（可再大，数据小无必要） | 提高吞吐与 BN 稳定性 |
| 30 epoch 单变体 | 30~68 min | 预计 <5 min | 整轮实验矩阵小时级完成 |
| 多 seed | 串行、每个 ~1h | 可 3 seed 并行（多进程）或串行很快 | 复现成本大降 |
| 预训练/生成模型 | 不可行 | ✅ 解锁 | P2a / 生成式增强 |
| 死锁风险 | focal 曾 swap 死锁 8h | 无此风险 | 大显存 + 无 swap 颠簸 |

典型命令（H100）：
```bash
cd $REMOTE/scripts
DEVICE=cuda BATCH=256 SEED=42  ONLY=tokenattn python -u train_c1c2.py
DEVICE=cuda BATCH=256 SEED=123 ONLY=tokenattn python -u train_c1c2.py
DEVICE=cuda BATCH=256 SEED=2024 ONLY=tokenattn python -u train_c1c2.py
```

### 3.4 冒烟测试（确认环境 OK，~2 分钟）
```bash
cd $REMOTE/scripts && DEVICE=cuda EPOCHS=1 BATCH=256 ONLY=bilstm python -u train_c1c2.py
# 期望：正常跑 1 epoch 并落盘 docs/c1c2_bilstm_results.json（数值无所谓，通路通即可）
```

---

## 四、本地整理建议（清单先行，逐项确认后执行）

> 原则：先列清单，不自动删除。以下均为低风险/可逆操作，你确认哪几项我就执行哪几项。

### 4.1 可直接清理（垃圾/重复/已废弃，预计释放 ~3 MB）
- [ ] `scripts/__pycache__/`、根目录与 docs 下 `.DS_Store`
- [ ] `docs/_tmp_transformer_y_test_windows.npy`（62 KB 临时文件）
- [ ] `docs/fusion_v2_*_断点续训备份.*`（3 个文件 ~2 MB，bug 产物，结论已废弃，手册已说明勿引用）
- [ ] `docs/fusion_gradcam_*` / `fusion_shap_*` / `fusion_sensitivity.*` 等旧融合模型可解释性产物（若论文已不用旧 fusion 图）

### 4.2 候选压缩归档（释放 ~260 MB，移到外置盘/网盘，不删除）
- [ ] `A dataset of EV batery charging...zip`（197 MB，与 `dataset /` 解压内容重复，保留一份即可）
- [ ] `归档.zip`（62 MB，内容未知——建议先 `unzip -l 归档.zip | head` 确认是什么再归档）

### 4.3 需你决策/不动
- `dataset /` 两个 xlsx（209 MB）：**保留本地**（H100 只同步 A 类即可；若担心丢，归档到网盘）
- `.git/` 401 MB：建议 `git gc --aggressive` 瘦身或 push 到远端（GitHub/CNB），本地不删
- `data/real/seq_*` v1 旧版 parquet：保留（有脚本引用，勿删）

### 4.4 执行顺序建议
1. 先做 4.1（即时释放）→ 2. 跑第二节 rsync 同步到 H100 → 3. 在 H100 上按第三节搭建 + 冒烟 → 4. 4.2 归档大 zip 到外置盘（可选）。

---

## 五、H100 迁移后第一批实验建议（与《方向分析与实验方案》衔接）

见方案文档 4.2 实验矩阵。H100 上建议顺序：**P0a 集成 → P0b 检验 → P0c 特征验证 → P0d TabPFN → P1a 数据增强**。P0a/P0b 纯计算（用已同步的 3 个 `*_prob.npy`，无需训练即可先出结果，甚至 Mac 本地就能跑）。
