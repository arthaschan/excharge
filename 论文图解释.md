# 论文 8 张图通俗解释

> 把论文里的 8 张图按"它们在解决什么问题 → 图上每条线/每个柱子/每种颜色在说什么 → 怎么读"三个层次讲透。
> 图编号与 `paper/paper_draft.md` 完全对应。

---

## 总览：8 张图分三组

| 组 | 图编号 | 共同回答的问题 |
|---|---|---|
| **数据诊断** | 图 1 | 故障长什么样？（先认识数据） |
| **模型性能** | 图 2 | 整体判得准不准？ |
| **可解释性** | 图 3、4、5、6、7 | 凭什么判为故障？（时序分支 + 特征分支） |
| **鲁棒性** | 图 8 | 模型对输入稳不稳定？ |

---

## 图 1：故障指纹（Fault Fingerprint）

**回答**：在数据层面，故障样本和正常样本**到底有什么物理差别**？

**怎么读**：左右两个子图，都是"正常（蓝）vs 故障（红）"成对柱状图；柱越高 = 该特征均值越大；红色数字 = 故障均值 − 正常均值。

**关键发现**：

| 特征 | 故障 - 正常 | 物理含义 |
|---|---|---|
| 末端 SOC | **−15.6 pp** | 故障车没充满就停了（充电未完成）|
| SOC delta | −12.4 | 同上，电池只充进去一半 |
| 末端功率 | **+26.9 kW** | 故障瞬间功率还很高（突然中断）|
| 均值功率 | +13.0 | 故障车平均功率更高（高功率段出事）|
| 充电时长 | −25.2 min | 故障充电更短（被中断）|
| 总能量 | −8.5 kWh | 总充入更少 |
| 枪温 1 | −2.8 ℃ | 枪温反而略低（因充电短，没热起来）|
| 枪温 2 | −10.3 ℃ | 同上 |
| 电压（最小/均值/最大）| +7.1 / +0.4 / **−7.4** V | 故障**最高电压更低**（电池没充满，电压没到峰值）|

**结论（这条故障指纹很重要）**：故障 = **"高功率充电过程中突然中断"** → 表征是"末端功率高 + 末端 SOC 低 + 时长短"。

注意电压的差异**很小**（几个伏特），这解释了为什么纯 BiLSTM 难做——电压信息不够区分，要靠功率/温度/时长的组合来识别。

---

## 图 2：ROC 与 PR 曲线

**回答**：模型整体判得准不准？

**怎么读**：

**左图 ROC**（Receiver Operating Characteristic）
- 横轴 = 误报率 FPR（把正常判成故障的比例）
- 纵轴 = 召回率 TPR（把故障抓出来的比例）
- 曲线越**贴近左上角** = 越好
- 虚线 = 随机瞎猜（AUC=0.5）
- **曲线下面积 = AUC**（越大越好）

**右图 PR**（Precision-Recall）
- 横轴 = Recall
- 纵轴 = Precision
- 曲线越**贴近右上角** = 越好
- 虚线 = 随机基线 = 故障率本身（0.0465，因为 4.65% 样本是故障）
- **曲线下面积 = PR-AUC**（不平衡场景下比 AUC 更真实）

**论文里的结果**：

| 模型 | AUC（左图）| PR-AUC（右图）|
|---|---|---|
| 融合（红线）| 0.978 | 0.748 |
| 纯 Bi-LSTM（蓝线）| 0.908 | 0.351 |
| 随机（虚线）| 0.500 | 0.0465 |

**结论**：融合模型两条曲线都在纯 BiLSTM 上面，且**右图差距比左图更明显**（0.748 vs 0.351，约 2 倍）——这正是 PR-AUC 在不平衡场景下更"严"、更能反映真实差距的体现。

---

## 图 3：1D-GradCAM 通道重要性

**回答**：模型在做判别时，**时序分支**主要"看"了哪个传感器？

**怎么读**：水平条形图，长度 = 这个通道对"判为故障"的贡献大小（数学上是对数几率对输入梯度的绝对值之和）。红色 = 排第一的"突出项"。

**论文里的结果**（故障样本 n=129 的平均）：

| 排名 | 通道 | 重要性 |
|---|---|---|
| 1 | **枪温 1** | **0.47** |
| 2 | 枪温 2 | 0.40 |
| 3 | SOC | 0.26（红色高亮）|
| 4 | 电压 | 0.22 |
| 5 | 功率 | 0.20 |
| 6 | 电流 | 0.20 |

**结论**：时序分支把"枪温"和"SOC"放在前两位，**电气通道（电压/电流/功率）靠后**。

这看起来反直觉（电气通道不是更直接吗？）——因为 62 维手工特征分支**已经把电气信息吃透了**（看 SHAP 图 6 就知道 v_min/a_min/p_max 排前几），**留给时序分支的边际贡献自然转移到温度动态**。这正是"双轨互补"——双分支不会重复劳动。

---

## 图 4：1D-GradCAM 时间维度重要性

**回答**：时序分支的注意力**集中在充电的哪一段时间**？

**怎么读**：左图 = 从充电开始计时，右图 = 把每条序列**末端对齐**（时间 0 = 充电结束时刻），这样不同长度的序列能叠在一起比。曲线越高 = 该时间点贡献越大。红色 = 故障，蓝色 = 正常。

**关键发现**：

| 视角 | 故障样本 | 正常样本 |
|---|---|---|
| 绝对时间（左）| 在 t≈40-50（充电中段）有**小尖峰** | 在 t≈40 处**大宽峰**（峰值远高于故障）|
| 末端对齐（右）| **几乎为 0** | 末端 0-20 和中段 20-30 有**两个峰** |

**结论（重要！**这个图反直觉，但有解释）：

- 直觉：故障是"末端突然中断"，应该归因集中在末端 → 实际**没有**。
- 真实情况：故障归因在**充电中段（t≈40-50 步）**有尖峰，**末端反而归因为 0**。
- **解释**：因为末端"突然中断"这个**事实**已经被 62 维手工特征（如 soc_last、end power）精确编码进了特征分支；时序分支负责的是"**这个中断过程是怎么发生的**"——即**触发时刻**的瞬时模式（中段异常），而不是"中断后"的状态。
- **正常样本**有末端和中段两个峰，是因为正常充电有**两个特征性阶段**（恒流 → 恒压切换），而故障样本没有"恒压切换"这个正常模式，所以末端归因为 0。

简单说：**时序分支抓的是"中段出了什么异常"，特征分支抓的是"末端是什么状态"**——各管一段，互补。

---

## 图 5：1D-GradCAM 单样本热力图

**回答**：对一个具体的故障样本，每个通道**每个时间点**的归因强度是多少？

**怎么读**：
- **上子图**：那条故障充电的实际 6 通道曲线（标准化的，y 轴是 z-score 后的值）。
- **下子图**：热力图，**纵轴 = 6 个通道**，**横轴 = 时间步**，**颜色 = 归因强度**（白=0，红=1，越深红=贡献越大）。

**论文里的例子**（序列 #1429，长度 L=200）：
- 上图能看到约 **t≈145 时刻**，功率（绿）有个**明显阶跃下降**——这就是"突然中断"事件。
- 下图热力图：
  - 枪温 1 在 **t=0-10**（最开始）和 **t=190-200**（末端）有两块深红。
  - 枪温 2 在 t=0-10 也有明显红色。
  - 其他通道（电压/电流/功率/SOC）整体较白（贡献小），零散在 t=145 附近有少量红色。

**结论**：
- 时序分支对**起始段**特别敏感（可能因为电池启动时枪温的瞬态特征是关键判据）。
- 对**事件发生时刻**（t≈145）有一定归因，但更弱——因为"中断事件"已被特征分支吸收。
- **这张图的价值**：给运维人员一个"可定位到具体传感器 + 具体时间段"的诊断工具——审稿人最关心的"可解释"就在这张图上落地。

---

## 图 6：SHAP 全局特征重要性（Top 15）

**回答**：**特征分支**做判别时，**62 维手工特征里谁最重要**？

**怎么读**：水平条形图，长度 = 该特征 SHAP 值的绝对值平均（mean |SHAP|）。SHAP 越大 = 该特征对预测的贡献越大。**横轴不是 SHAP 原始值，是绝对值**——所以越靠前的特征 = 在所有样本上"贡献都大"。

**论文里的 Top 15**：

| 排名 | 特征 | 含义 | mean \|SHAP\| |
|---|---|---|---|
| 1 | **t2_mean** | 枪温 2 平均温度 | 1.55 |
| 2 | t2_max | 枪温 2 最高温度 | 1.03 |
| 3 | t1_mean | 枪温 1 平均温度 | 0.83 |
| 4 | **v_min** | 最低电压 | 0.68 |
| 5 | **a_min** | 最小电流 | 0.65 |
| 6 | **soc_last** | 末端 SOC | 0.63 |
| 7 | t2_std_2nd | 枪温 2 后半段波动 | 0.50 |
| 8 | a_seg_change_2to3 | 电流中后段变化 | 0.45 |
| 9-15 | 略（t1_max、t2_last、v_mean、a_last_third_max、p_mean、v_max、p_last）| ... | 0.30-0.43 |

**关键发现（与图 1 故障指纹交叉印证）**：
- **枪温三件套**（t2_mean/t2_max/t1_mean）占 Top 3 —— 与故障指纹"枪温异常"一致。
- **电气三件套**（v_min/a_min/soc_last）紧随其后 —— 与故障指纹"末端功率高 + 末端 SOC 低"一致。
- SOC 排第 6，枪温排第 1-3——和直觉有点反（以为电气更重要），但因为枪温统计量在大量故障样本上确实差异大（图 1 枪温 1 差 −2.8、枪温 2 差 −10.3，跨样本方差大 → SHAP 贡献也大）。

**结论**：SHAP 给出了和图 1 物理指纹一致的归因——模型学到了**有物理意义的判别特征**，不是"黑箱乱判"。

---

## 图 7：SHAP 故障 vs 正常样本特征对比

**回答**：故障样本和正常样本**各自的 Top 12 判别特征**有什么异同？

**怎么读**：左右两个子图各 Top 12 水平条形图。**左边（红）= 故障样本**，**右边（蓝）= 正常样本**。对比两边的"特征排名差异"。

**关键差异**：

| 排名 | 故障样本看重的（红）| 正常样本看重的（蓝）|
|---|---|---|
| Top 4 | t2_mean, t1_mean, t2_max, **soc_last** | t2_mean, t2_max, t1_mean, **v_min** |
| 中段 | v_min, t1_max_jump, t2_max_jump | a_min, **soc_last**, t2_std_2nd |

**两个关键差异**：

1. **故障样本**把 **soc_last（末端 SOC）** 推到 Top 4（红图第 4），而**正常样本**把 **soc_last** 放在第 6（蓝图第 6）—— 故障更看重"末段是否充满"。
2. **正常样本**把 **a_min（最小电流）** 推到 Top 5（蓝图第 5），而**故障样本**没把它放进 Top 6—— 正常更看重"电流是否跌到某个值"（恒压阶段特征）。

**结论（验证双轨互补的"故障指纹"**）：
- 故障判别 = 看重 **"末端 SOC 是否达到"**（特征分支）+ 看重 **"枪温异常"**（时序分支）。
- 正常判别 = 看重 **"电流是否进入恒压段"**（特征分支）+ 看重 **"中段切换模式"**（时序分支）。

两边的 Top 特征**高度重合但排序不同**——说明模型对"故障"和"正常"分别有**不同物理意义的判据**，不是简单的"反着判"。

---

## 图 8：输入通道敏感性分析

**回答**：把**时序分支的某个通道置零**（强行让模型"看不见"它），模型性能会变多少？

**怎么读**：左右两子图，**左 = Δ Recall，右 = Δ AUC**；横轴 = 被置零的通道；**红色 = 性能下降，蓝色 = 性能上升**。条形越长 = 性能变化越大。

**关键发现**：

| 通道 | Δ Recall | Δ AUC |
|---|---|---|
| 电压 | **−0.008**（微降）| +0.001（微升）|
| 电流 | +0.023（升）| −0.003 |
| 功率 | +0.023（升）| −0.0002 |
| 枪温 1 | +0.008（升）| **−0.007（最大降）**|
| 枪温 2 | +0.015（升）| −0.006 |
| SOC | **+0.070（最大升）**| −0.002 |

**最重要的发现**：

- **整体变化都极小**（Recall 变化 < 0.07，AUC 变化 < 0.007）→ 融合模型**对单通道鲁棒**。
- 这和**纯 BiLSTM 完全不同**（纯 BiLSTM 把电压置零会 Recall 暴跌 37.98pp）。
- 原因：**62 维手工特征分支已经承担了主要判别**，时序通道被移除时特征分支**自动补上**。
- 异常点：**SOC 置零反而让 Recall 上升 0.07**——可能时序分支对 SOC 的"过度敏感"产生了一点噪声，去掉它反而纯净。

**结论**：
- **融合模型鲁棒性强**（§4.5 重要结论）。
- **强鲁棒本身是可解释的**——因为双轨互补：特征分支扛主要，序列分支扛补充。
- 但**过鲁棒也值得警惕**：单通道贡献太小，可能模型**没充分利用时序信息**——这是 §5.3 论文已经如实指出的局限。

---

## 总结：8 张图的整体逻辑

| # | 主题 | 回答的问题 | 关键证据 |
|---|---|---|---|
| 1 | 故障指纹 | 故障长什么样 | 末端 SOC 低、末端功率高、时长短 |
| 2 | ROC/PR | 整体性能 | 融合全面超过纯 BiLSTM |
| 3 | GradCAM 通道 | 时序分支看哪个传感器 | 枪温、SOC 居前 |
| 4 | GradCAM 时间 | 时序分支看哪段时间 | 中段（t=40-50）有尖峰 |
| 5 | GradCAM 热力图 | 单样本具体定位 | #1429 在 t=145 突然中断 |
| 6 | SHAP 全局 | 特征分支看哪些特征 | 枪温三件套 + v_min/a_min/soc_last |
| 7 | SHAP 对比 | 故障 vs 正常特征差异 | 故障更看 soc_last，正常更看 a_min |
| 8 | 敏感性 | 模型对输入稳不稳 | 单通道置零几乎无影响 → 双轨互补 |

**一句话总结整组图**：**数据上（故障指纹）→ 性能上（ROC/PR）→ 解释上（GradCAM 时序 + SHAP 特征）→ 鲁棒上（敏感性），构成"诊断—性能—可解释—可靠"四层闭环**——这就是论文卖给 TAIG（AI 治理会议）的核心。
