# 时序动力学特征提取方法说明

> 对应导师批注 4：摘要中「20 维时序动力学特征」表述模糊，需说明具体提取方法。
> 本说明基于实际代码 `scripts/build_enhanced_features.py`（`enhanced_features()` 函数），逐条列出方法、逐个 feat key 计数，**不夸大、不虚构**。

---

## 一、诚实结论（含 62 维准确拆分）

62 维手工特征的实际分类（按 `enhanced_features()` 返回的 dict keys 逐个数过）：

| 类别 | 维数 | 特征 |
|------|------|------|
| 基础统计量 | **32** | v_mean/std/min/max/first/last/slope, a_*(6), p_*(6), soc_first/last/delta(3), t1_mean/max/t2_mean/max/t1_last/t2_last(6), n_points/duration_min/total_kwh/p_v_ratio(4) |
| 一阶差分/导数 | **13** | t1_slope, t2_slope, t1_max_jump, t2_max_jump, p_max_jump, soc_rate, soc_last_rate |
| 分段均值/端点 | **8** | v_seg_change_1to2/2to3, a_seg_change_1to2/2to3, a_seg3_max, v/a/p_last3_vs_first3 |
| 超温指示 | **4** | t1_over_40, t2_over_40, t1_over_45, t2_over_45 |
| 电池类型独热 | **5** | bt_LFP, bt_NMC, bt_LMO, bt_LCO, bt_LP |
| **合计** | **62** | |

### 摘要里 "37+20+5" 的错误

新论文摘要沿用了**旧 XGBoost 摘要**的拆分（"37 维统计 + 20 维时序动力学 = 57"），再拼上电池 5 维凑成 62。但实际代码中：

- 基础统计是 **32 维**（不是 37）—— 旧摘要的 37 不知怎么数出来的，对不上当前代码。
- "时序动力学"（差分+分段）是 **21 维**（13+8），"20" 是粗略取整。
- 总数 62 对，但**拆分口径是错的**，且 37+21+5=63 ≠ 62，自相矛盾。

修正写法：

> 62 维手工特征：32 维基础统计量、13 维一阶差分/导数特征、8 维分段均值与端点对比特征、4 维超温指示、5 维电池类型独热编码。

---

## 二、提取方法（按代码逐条列出）

上述「一阶差分/导数 + 分段均值/端点」共 21 维，由三类简单操作构成（**非正规时序方法**）：

### ① 一阶差分 / 导数类（13 维）

| 特征 | 公式 | 说明 |
|------|------|------|
| `t1_slope` | (枪温1末值 − 首值) / (n−1) | 温度变化率 |
| `t2_slope` | (枪温2末值 − 首值) / (n−1) | 温度变化率 |
| `t1_max_jump` | max\|Δ枪温1\| | 相邻点最大跳变 |
| `t2_max_jump` | max\|Δ枪温2\| | 相邻点最大跳变 |
| `p_max_jump` | max\|Δ功率\| | 功率最大突变 |
| `soc_rate` | mean(ΔSOC) | SOC 平均增速 |
| `soc_last_rate` | 末段 ΔSOC | 末端 SOC 变化速率 |

### ② 滑窗 / 分段统计类（6 维）

将序列按时间段切分后取极值/统计量（**粗粒度分段**，非重叠滑窗）：

| 特征 | 公式 | 说明 |
|------|------|------|
| `t1_std_2nd` | std(枪温1后半段) | 充电峰值段温度波动 |
| `t2_std_2nd` | std(枪温2后半段) | 充电峰值段温度波动 |
| `v_first_third_min` | min(电压前 1/3) | 起始段电压谷值 |
| `v_sag_from_mean` | 电压均值 − 前 1/3 最小值 | 「电压跌落」幅度 |
| `a_last_third_max` | max(电流后 1/3) | 末段电流峰值 |
| `a_first_last_ratio` | 电流末值 / 首值 | 断充模式（末值骤降） |

### ③ 分段均值变化 + 端点对比类（8 维）

序列三等分后比较段间均值，以及首末各 3 点的对比：

| 特征 | 公式 | 说明 |
|------|------|------|
| `v_seg_change_1to2` | mean(电压段2) − mean(电压段1) | 段间电压变化 |
| `v_seg_change_2to3` | mean(电压段3) − mean(电压段2) | 段间电压变化 |
| `a_seg_change_1to2` | mean(电流段2) − mean(电流段1) | 段间电流变化 |
| `a_seg_change_2to3` | mean(电流段3) − mean(电流段2) | 段间电流变化 |
| `a_seg3_max` | max(电流段3) | 末段电流最大值 |
| `v_last3_vs_first3` | mean(电压末3点) − mean(电压首3点) | 首末端点对比 |
| `a_last3_vs_first3` | mean(电流末3点) − mean(电流首3点) | 首末端点对比 |
| `p_last3_vs_first3` | mean(功率末3点) − mean(功率首3点) | 首末端点对比 |

---

## 三、明确排除的方法（均未使用）

- ❌ FFT / 频域特征
- ❌ 小波变换（wavelet）
- ❌ 变点检测（CUSUM、Bayesian change-point）
- ❌ ARIMA / 状态空间模型
- ❌ 自相关 / 偏自相关函数

---

## 四、诚实评价

1. **导师批注 4 问得对**：摘要「20 维时序动力学」表述模糊，且拆分（37+20+5）与代码实际（32+21+4+5）对不上。
2. 本质是**手工启发式**：上述 21 维是围绕「充电突然中断」这一物理模式（大电流高功率段骤停、末端 SOC 未充满、温度未充分累积）手工设计的统计量，不是数据驱动或正规时序方法。
3. **修正项**：
   - 摘要「37 维统计 + 20 维时序动力学 + 5 维电池类型」→ 改为「32 维基础统计量、13 维一阶差分/导数、8 维分段均值与端点对比、4 维超温指示、5 维电池类型独热编码 = 62」。
   - 论文 §3.2「62 维手工特征...包括：(1)基础统计量...;(2)时序动力学特征...」保持大类描述，但**把"时序动力学"展开为方法描述**（已写明差分/最大跳变/电压跌落/分段变化/端点对比），与本说明一致。

---

*本文档可作为对导师批注 4 的补充说明附上，同时可作为摘要/§3.2 拆分表述的修正依据。*
