# 模型动物园复盘：从 0.351 到 0.918 的系统学习笔记

> **写给谁**：这篇笔记给"刚做完论文、想真正搞懂自己用过的每个模型"的你——一个想借这篇论文完成一次系统性回溯的 AI 硕士。
> **素材来源**：本项目真实实验记录（AUTORUN 实验报告 / 实验探索全记录 / HANDOFF 交接文档 / 论文终稿），所有数字均为落盘真实值。
> **怎么读**：Part 0 给全景地图 → Part 1 给看问题的框架（先于模型本身）→ Part 2 是逐模型讲义（正文）→ Part 3 回答"为什么一开始没想到"（你最想知道的）→ Part 4 把教训变成可迁移的方法论 → Part 5 是继续深造的路线图。
> **学习目标**：读完后你能做三件事——① 给每个用过的模型讲清"它是什么、天生适合什么问题"；② 说清本项目每个失败/成功背后的机制原因；③ 面对一个全新任务，能复用它选型的心智流程。

---

## Part 0｜全景图：你其实打过一场"五大家族战争"

先看终局成绩（主指标 PR-AUC，跨站点 owner1-6 训练 → owner7-8 测试）：

| # | 模型/方法 | 家族 | PR-AUC | 结局 |
|---|----------|------|--------|------|
| 1 | XGBoost（57 维旧特征，早期） | GBDT | 0.893 | 口径不同，被导师否 |
| 2 | XGBoost（62 维，H100） | GBDT | **0.887** | 参照天花板 |
| 3 | LightGBM（62 维） | GBDT | **0.868** | 参照天花板 |
| 4 | 纯 Bi-LSTM（端到端 6 通道） | 监督深度 | 0.351 | ❌ 暴跌 |
| 5 | LSTM-AE 重建式 | 重建式 | 0.140 | ❌ 失败 |
| 6 | CORAL 域自适应 | 域自适应 | 0.825 | ❌ 收益≈0 |
| 7 | BiLSTM+62 特征简单拼接 | 监督深度 | 0.748(假)→0.829 | ⚠️ bug 修正后真值 |
| 8 | iTransformer / PatchTST / FT-Transformer / Dual-Transformer | 换编码器 | 0.592 / 0.744 / 0.716 / 0.275 | ❌ 全灭 |
| 9 | 多尺度池化（C1） | 融合强化 | 0.806 | ❌ 无增益 |
| 10 | Gated 门控（C2） | 交互融合 | 0.796 | ❌ 退步 |
| 11 | Cross-Attn（C2） | 交互融合 | 0.845 | ✅ 有效 |
| 12 | **Token-Attn（C2）** | 交互融合 | 0.851 → 0.874±0.033 | ✅ **主模型** |
| 13 | **Token-Attn 7-seed 概率集成** | 集成 | **0.918** | 🏆 最终 |
| 14 | 数据增强 / 去趋势残差化 | 数据策略 | 0.815~0.867 | ❌ 负结果 |

**一句话看懂整个故事**：这个任务的知识主要藏在 62 维统计特征里（不是原始曲线里），所以"纯深度吃原始信号"(0.351) 和"换更强序列编码器"都输给了"特征直接喂树"（GBDT 0.868）；最终翻盘靠的不是更强的模型，而是**用注意力把"序列"和"特征"放进同一个空间互相查阅**（Token-Attn），再靠 **seed 集成**（+0.045）把强不平衡下的高方差压下去——最后以 0.918 反超 GBDT，还白送一份注意力可解释性。

---

## Part 1｜先建立心智框架：比记模型更重要的一件事

### 1.1 先做"任务解剖"：你解决的到底是一道什么题

在谈任何模型前，先给任务定性。本项目数据（深圳 30 站，155.6 万采样点，19,658 条充电序列）呈现四个关键属性，**这四个属性决定了后面所有模型的生死**：

| 属性 | 本项目情况 | 它决定了什么 |
|------|-----------|-------------|
| ① 有没有标签 | **有**（642 条标注故障） | 重建式/无监督是"自废武功"，监督路线才是主场 |
| ② 正样本稀有度 | 序列级故障率 **4.65%**，只有 129 条测试故障 | 深度模型必然高方差 → 需要 seed 集成 |
| ③ 训练/测试分布是否一致 | **不一致**：owner1-6 训练 → owner7-8 全新站点 | 端到端学到的"时序模式"会不迁移 |
| ④ 可解释性是否刚需 | **是**（EU AI Act / TAIG 主题） | GBDT 再强也当不了主模型（黑盒） |

而最容易被忽略、事后证明最关键的一条隐藏属性是：

> **⑤ 判别信息藏在哪？**——藏在"整条曲线的统计规律"（充了多久、末端功率衰减没有、枪温峰值多少、SOC 充满没有）里，而不是"曲线每一时刻的瞬时形状"里。

这一条没有写进任何实验设置里，却是整场战争的胜负手。62 维特征为什么那么强？因为它把 200 步原始曲线"蒸馏"成 62 个高信息密度的统计量；原始 6 通道曲线里，同样的信息被稀释在 200×6=1200 个数值中，且混着大量与故障无关的逐点噪声。**对深度学习而言，信息密度决定一切。**

### 1.2 五大家族的世界观（每个家族=一套对"异常怎么检测"的根本假设）

| 家族 | 代表 | 根本假设（世界观） | 隐含前提 | 前提在本项目成立吗 |
|------|------|------------------|---------|:---:|
| **监督分类** | GBDT / 深度分类器 | "我有标签，我直接学'正常 vs 故障'的分界线" | 有足够的标注 | ✅ |
| **重建式** | LSTM-AE / VAE / USAD | "异常=重建不好；正常=能被压缩重建" | **无标签** 且 异常是结构性大偏差 | ❌（有标签，且异常是微弱偏移，AE 能把它也平滑重建） |
| **域自适应** | CORAL / DANN | "跨域失效=分布不一致，对齐分布即可" | 源/目标域**标签函数一致**，只是输入分布漂移 | ⚠️（新站点的故障签名本身可能不同，对齐输入分布没用） |
| **树模型（GBDT）** | XGBoost / LightGBM | "特征已经提取好，我负责找特征间的非线性组合" | 特征工程靠谱 | ✅（62 维特征极其给力） |
| **集成/统计** | seed ensemble / bootstrap | "单个模型不稳定，用多次重复换稳定" | 模型有方差 | ✅（深度模型方差大得惊人） |

> **核心提醒（先纠正一个常见误区）**：不是"深度模型 > 树模型"或反之，而是**每种家族都带着自己的前提假设来解题**。选错家族 = 前提不成立 = 再努力也白搭。你后面会看到：项目里所有大失败（重建式 0.14、域自适应≈0）都是"前提不成立却硬套范式"，而不是模型本身差。

---

## Part 2｜逐模型讲义：是什么、擅长什么、为什么在这里栽了

> 每个模型统一按四问讲：**它是什么 → 天生擅长什么 → 在本项目的结果 → 为什么（机制层面）**。数字全部真实。

### 2.1 GBDT 家族：XGBoost / LightGBM —— 表格数据的"天花板"

**它是什么**：梯度提升决策树（Gradient Boosting Decision Tree）。核心思想是**接力纠错**：第一棵树给个粗糙估计，第二棵树专门学第一棵的"错误残差"，第三棵学前两棵的残差……几百棵树串成一条纠错流水线，每棵都是简单规则（if-else 分裂），合起来能拟合极复杂的非线性。LightGBM/XGBoost 是它的两个高效实现（直方图加速、叶节点生长策略等工程优化）。

**通俗类比**：一屋子实习生接力改论文——第一个人写初稿（质量糙），第二个人只改他改错的地方，第三个人只改前两个人遗留的错误……每个人水平都一般，但接力几十轮后错误几乎清零。单棵决策树=只会几条规则的"笨实习生"，几百棵接力=聪明团队。

**天生擅长什么**：**表格/特征数据**（一行样本 = 几十上百个数字特征）。特点：① 对特征数值范围不敏感（树分裂只看排序，不怕量纲不一）；② 自动发现特征间的非线性交互；③ 训练极快、可并行；④ 对类别不平衡有成熟武器（`scale_pos_weight`/正样本权重）；⑤ 小样本下表现极稳（没有随机初始化，几乎没有方差）。

**在本项目的结果**：
- XGBoost 62 维：Mac 0.861 / H100 0.887（backend 版本差异，诚实口径取 H100 0.887）
- LightGBM 62 维：0.868（Mac/H100 逐位一致 0.8684）
- XGBoost 57 维旧特征（早期，owner1-4 口径）：0.893

**为什么它是天花板 / 深度打不过它**：因为判别信号全在 62 维特征里，而树模型就是为"吃特征"而生的——它不浪费任何一个统计量的信息。当时的深度模型只有两条路：要么纯吃原始 6 通道（0.351，信息被稀释），要么把特征拼接进网络（融合早期 0.829，交互方式粗糙）。树模型直接、高效、无方差地消费了这批高密度特征。

**它的局限（在本项目中为什么不能当主模型）**：
1. **可解释性弱**：树的判断是"一堆 if-else 规则的叠加"，无法回答"这个样本为什么异常？它异常时模型在关注充电进程的哪一段？"——TAIG/EU AI Act 卖点直接归零；
2. 输出的是特征空间的分数，**没有"序列×特征"的交互视角**——它看不出"枪温特征正在查阅启动段的电流"这种跨模态关系（这是 Token-Attn 白送的能力）。
3. 导师明确要求端到端深度模型（批注 1：XGBoost 老旧）——这是外部约束，不是技术问题。

> 📌 **可带走的能力**：凡是"特征工程扎实的表格型预测"，先跑一个 LightGBM 当"免费天花板"——它 1 秒出结果，能立刻告诉你"这活儿的天花板有多高"，避免在深度模型里盲目挣扎。你论文里 0.868 这个参照系就是这么起作用的。

### 2.2 纯 Bi-LSTM（端到端）—— 0.351 暴跌的教训

**它是什么**：双向长短期记忆网络。LSTM 是循环神经网络（RNN）的升级版，用"门"机制（遗忘门/输入门/输出门）决定每个时刻该记住什么、忘掉什么，从而能捕捉长距离依赖；双向（Bi）就是正向读一遍+反向读一遍，把两个方向的记忆拼起来。

**通俗类比**：LSTM 像一个"一边读一边做笔记"的读者——每读一个新词（新时刻），他会翻自己之前的笔记决定"该更新的重点是什么"。普通 RNN 记久了会忘（梯度消失），LSTM 的门控笔记术解决了"长期记忆"问题；双向版本再倒着读一遍，把"这句话最后才出现的关键信息"也能提前感知。

**天生擅长什么**：**逐点过程模式**——信号的意义均匀分布在每一个时间步上、且前后依赖重要的任务：语言建模、语音识别、动作/手势识别、机器翻译、按时间顺序展开的生理信号分类。它的天赋是"跟着时间走、记住上下文"。

**在本项目的结果**：PR-AUC **0.351**（AUC 0.908）。是 LightGBM(0.868) 的 40%、最终模型(0.918) 的 38%。

**为什么这么差（机制层面，三层）**：
1. **信息密度错配（主因）**：故障判别靠的是"整条曲线的统计规律"（充了多久、末端功率是否衰减、SOC 是否充满），而 LSTM 的强项是逐点过程记忆。它把 200 步×6 通道的原始数值一个点一个点地"读"过去，判别信号被稀释在大量与故障无关的逐点波动里——**它擅长读"过程小说"，而这题考的是"统计学结论"**。
2. **变长序列+padding**：序列长度 30~200 不等，padding 部分引入大量无意义步长，模型要额外学会"忽略 padding"，浪费容量。
3. **跨站不迁移**：owner1-6 学到的"逐点时序模式"在 owner7-8 新站点上分布漂移（设备型号/用户习惯不同），逐点模式失效；而"统计规律"（没充满=异常）这种物理常识反而跨站成立——这正是特征分支后来胜出的深层原因。

> 📌 **可带走的能力**：拿到时序任务先问"判别信息在过程里还是在统计里"——如果是"语音/手势/动作"这类**过程性**任务，LSTM/GRU 是主场；如果是"一段过程结束后的状态判断"（充满没？衰减没？超温没？），**统计特征 + 分类器**往往碾压端到端序列模型。端到端深度模型不是不好，是**不能让它独自消化低信噪比的原始信号**。

### 2.3 LSTM-AE 重建式 —— 世界观与任务错位

**它是什么**：自编码器（AutoEncoder）= 一个"压缩再还原"的机器：编码器把输入压成低维瓶颈向量，解码器从瓶颈还原输入。训练时**只用正常样本**，让模型学会"把正常数据还原得很好"。检测时把新样本喂进去，重建误差大 = 异常（模型没见过这种模式，还原不了）。

**通俗类比**：让一个只见过"正常苹果"的画师临摹水果。你给他一个正常苹果，他画得惟妙惟肖（误差小）；你给他一个"长了虫眼的苹果"，他从没见过，画出来歪歪扭扭（误差大）——拿"画得像不像"当异常检测器。LSTM-AE 就是"画师"换成会读时序的版本。

**天生擅长什么**：**无标签场景下的结构性异常**——异常是"模式完全不同/剧烈突变"的场景：工业设备断轴、网络入侵流量突变、机器故障振动模式改变。典型成功案例里异常在特征空间离正常分布**很远**。

**在本项目的结果**：PR-AUC **0.140**（score_mean）/ 0.054（score_max）。略高于随机基线（0.047），基本没用。

**为什么失败（机制层面）**：
1. **异常太"微弱"、离正常分布太近**：本数据故障是"末端 SOC 没充满、功率没衰减、温度略异"这类**局部统计偏移**——在原始曲线层面，故障序列和正常序列长得非常像（都有一段正常的电压电流曲线），AE 的"压缩-还原"能力把这点微弱差异**当噪声平滑掉了**。它能重建"看起来正常"的故障序列 → 重建误差不显著。
2. **跨站正常基线漂移**：AE 只见过 owner1-6 的"正常"，owner7-8 新站点的正常序列本身就有差异 → 重建误差被站点差异污染，分不清"是异常还是只是站点不同"。
3. **浪费了标签**：明明有 642 条标注故障，重建式是纯无监督，等于主动扔掉最有价值的信息。

> 📌 **可带走的能力**：重建式三连问——① 我有标签吗？（有 → 别用重建式当主力）；② 异常和正常在原始信号上"长得像不像"？（像 → AE 会平滑掉它，这类"微弱偏移型"异常要监督/特征路线）；③ 正常本身在不同环境里稳不稳定？（不稳定 → 重建误差不可靠）。**文献热门的 USAD/Anomaly Transformer 和它是同一世界观，本数据上大概率同样失败**——这个推理你论文里已经写成了"已排除"的方法论记录。

### 2.4 CORAL 域自适应 —— 找错了"跨站失效"的病根

**它是什么**：CORAL（Correlation Alignment）是一种轻量域自适应方法：把源域（训练站点）和目标域（新站点）的**特征分布二阶统计量（协方差）拉齐**，让模型学到的表征"看不出是哪个域的"。实现上就是在损失函数里加一项 CORAL loss（lam 控制对齐强度），惩罚源/目标域表征协方差的差异。

**通俗类比**：两个班的学生水平分布不同（A 班平均分高、B 班低），你想训练一个对两班都公平的老师。CORAL 的做法是"先把两班分数都标准化到同样的均值方差再教学"——让老师看到的两个班"长得差不多"。

**天生擅长什么**：**标签函数一致、仅输入分布漂移**的迁移场景——比如同样的传感器型号装在两个工厂，读数分布不同但"故障=超阈值的定义"完全一样。此时对齐输入分布确实能帮模型泛化。

**在本项目的结果**：lam=0 → 0.8245，lam=0.5 → 0.8250（**差 0.0005，纯噪声**）。收益≈0。

**为什么失败（机制层面）**：
1. **"跨站"不是单纯的输入分布漂移，而是"标签函数也可能不同"**：owner7-8 是**全新站点**，故障签名（哪些组合算故障）可能与 owner1-6 不同。CORAL 对齐的是"输入长什么样"，但解决不了"什么算故障"的定义漂移——对齐了半天，判据本身还是不对。
2. **CORAL 只对齐二阶统计量（协方差）**，是浅层对齐；深度网络的表征是非线性、高维的，二阶对齐远远不够（DANN 这类对抗式方法更强但复杂得多）。
3. 深层原因同 2.2：判别主力其实在**特征**上，而 CORAL 实验把域对齐加在了融合模型上，特征分支的物理含义（统计量）跨站本来是稳的，不需要对齐——**在一个"本来就该稳"的地方强行对齐，自然没收益**。

> 📌 **可带走的能力**：域自适应不是跨域问题的万能钥匙。先诊断"失效来自输入漂移还是标签漂移"：如果新站点连"什么算异常"都变了（设备不同、故障定义不同），域自适应救不了，得靠**域无关的强特征**（物理统计量）或**更复杂的对抗式对齐**。本项目选择的道路（域无关特征 + 监督）恰好绕开了这个死结——事后看是更聪明的选择。

### 2.5 BiLSTM + 特征简单拼接 —— 两个 bug 掩盖了真相

**它是什么**：把 BiLSTM 编码出的序列表示（128 维）和 62 维手工特征**拼成一个向量**，再进分类层。这是"深度 + 特征"最朴素的融合——两个信息源各自编码后硬接在一起。

**通俗类比**：让"读曲线的专家"（BiLSTM）和"算统计的专家"（特征）各自写一份报告，然后**用订书机钉在一起**交给决策者。两个专家之间没有任何对话——订书机不会让专家互相提问。

**在本项目的结果（一段被 bug 扭曲的历史）**：
- **断点续训 bug**：早期为了规避超时把训练拆成"每 5 轮续跑"，但只存了模型权重、没存 AdamW 优化器状态 → 每次重启优化器从零热身 → 训练轨迹反复被打断 → 学到的表征过拟合源域 → 跨站崩 → 测出 **0.748**，一度被当成"深度天花板"。
- **完整训练后的真值：0.810~0.829**。同样的模型，完整训练直接 +0.08。0.748 是假象。

**为什么拼接（即使修好 bug）仍不是最优**：
1. 128 维序列表示 + 62 维特征**是两个不相通的向量空间**，模型要靠分类层自己"领悟"两者关系——高维空间里这种跨模态交互极难学，序列信息基本被"当噪声稀释"或"被强特征淹没"。
2. 没有结构化的交互通道：特征无法"选择性地查阅"序列的某一段（比如"枪温特征想看看充电末段的电流"——拼接结构里这种查询没有实现路径）。

> 📌 **可带走的能力**（两条血的教训）：
> ① **任何"训练数字"先问：完整训练还是续训的？不完整训练的数字不可信。**断点续训必须连优化器 state_dict 一起存。
> ② **"融合"不是"拼接"**——两个异构信息源（序列 vs 特征）合在一起时，**融合方式决定上限**。这是后面 C2 系列实验要解决的问题，也是本项目从"打平"走向"超越"的真正跳板。

### 2.6 换序列编码器四将：iTransformer / PatchTST / FT-Transformer / Dual-Transformer —— "换引擎"的陷阱

**它们是什么**（四个"更强"的编码器）：
- **iTransformer**：把 Transformer 倒过来用——以"变量"为 token（而非时间步），让每个变量的整条序列先各自编码，再用注意力建模变量间关系。适合**多变量强相关**的场景（如电网多传感器）。
- **PatchTST**：把时序切成小 patch（补丁）再进 Transformer，减少序列长度、保留局部模式，时序预测/分类 SOTA。
- **FT-Transformer**：把表格特征做成"特征 token"喂 Transformer，专为特征数据设计的深度表格模型。
- **Dual-Transformer**：双塔 Transformer 结构（本项目自定义的对照变体）。

**通俗类比**：你开车觉得慢，于是不停换发动机（1.5L→2.0T→V8）——但真正的问题是**油箱里装的燃料不对**（给发动机喂的是低密度信号），换再大的发动机也跑不快。iTransformer/PatchTST/FT/Dual 就是四台更大的发动机。

**在本项目的结果**（早期 train_newmodels.py，同框架早期早停协议）：
| 编码器 | PR-AUC | 相对 BiLSTM 基线 |
|--------|--------|----------------|
| Dual-Transformer | 0.275 | 崩 |
| iTransformer | 0.592 | 明显退步 |
| FT-Transformer | 0.716 | 偏低 |
| PatchTST | 0.744 | ≈基线 |

**为什么全军覆没（机制层面）**：
1. **燃料问题没解决**：它们吃的还是"6 通道原始序列"（或把 62 特征当普通表格），判别信息密度低的问题原封不动。PatchTST 只比基线略好是因为 patch 化恰好给了点"粗粒度统计"的甜头；iTransformer 反而更差——它的"变量间关系建模"假设多变量间有强交互，而本项目判别靠的是每通道**自身**的统计量（SOC 满没满、功率衰减没），跨变量交互并非主信号。
2. **小样本扛不住大模型**：FT-Transformer 这类深度表格模型需要海量数据学习特征交互；本项目只有 13,505 条训练序列，喂不饱它 → 欠拟合。
3. Dual-Transformer 崩（0.275）是典型的小样本+复杂结构=灾难。

> 📌 **可带走的能力（本项目最有价值的负结果之一）**：**当瓶颈在"喂什么信息"时，换更强的模型吃同样的信息是无效的**。模型选型的顺序应该是：先解决"信息入口"（特征/表征够不够好），再谈"模型容量"。四个编码器全灭这件事，反过来证明了"融合机制"才是本项目真正的杠杆——它把"燃料"（特征）和"引擎"（深度）正确地接上了。

### 2.7 交互融合三兄弟：Gated(0.796) / Cross-Attn(0.845) / Token-Attn(0.851) —— 正确杠杆的发现

这是整个项目从"追平"到"反超"的转折点。三个模型都做"序列×特征"融合，差别只在**交互方式**：

**① Gated 门控（0.796，退步）**
- **是什么**：学一个"门"（0~1 的权重向量），决定序列表示和特征表示各取多少，加权合并。
- **通俗类比**：两位专家各写一份报告，**领导（门）拍板"这次听 A 的 70%、B 的 30%"**——但领导只听汇报摘要，专家之间不对话。
- **为什么退步**：门控只是"软开关"，交互太浅——它决定"信谁多"，但不产生任何"新的联合信息"。序列和特征仍然各说各话，门控还引入了额外参数，小样本下纯增噪。**它证明"加了注意力/门控 ≠ 一定有效"，交互必须让信息真正流通。**

**② Cross-Attn 交叉注意力（0.845，有效）**
- **是什么**：让特征作为 query，去序列 token 里"查资料"（注意力查询），把查到的序列信息加权带回特征侧。
- **通俗类比**：让算统计的专家（特征）**主动去翻读曲线的专家（序列）的笔记**——"我想知道末段电流长什么样，你那段记了什么？"——跨模态信息第一次开始流通。
- **为什么有效**：单向但真实的交互——特征分支能"查阅"序列中与它相关的部分，信息开始双向流动，+0.016（相对拼接 0.829）。

**③ Token-Attn 多模态自注意力（0.851，本项目主模型）**
- **是什么**：把 `[CLS] 标记 + 8 个序列段 token + 62 个特征 token` 全部拼成一个 71 token 的序列，**送进同一个 Transformer 编码器做自注意力**。任意 token 可以关注任意 token——序列段可以查特征、特征可以查序列段、[CLS] 汇总全局。
- **通俗类比**：把两位专家和一位总裁（[CLS]）**请进同一间会议室**，每个人都能看到所有人的发言记录并互相提问——序列专家能问特征专家"你那个 SOC 没充满的结论靠谱吗"，特征专家能反问"你末段电流到底什么形状"，总裁最后综合全场发言拍板。**信息不再经过任何人的转述，全部在同构的注意力空间里直接对话。**
- **为什么最优（机制层面）**：
  1. **token 化统一了异构输入**：序列（原本是时间网格）被**分段池化成 8 个段 token**（保留"充电进程"粗粒度物理结构，抗逐点噪声）；特征本来就是 62 个语义单元，直接做 token。两类信息在同构空间里，交互成本最低。
  2. **自注意力 = 全连接交互**：不像 cross-attn 那样预先规定"谁查谁"，而是让数据自己决定谁该关注谁——更灵活，特征能"主动查阅"序列中与之最相关的充电段。
  3. **白送可解释性**：注意力权重本身就是"哪个特征在关注哪段充电进程"的证据（论文里 62 特征×8 段热力图），这正好命中 TAIG 卖点——**性能与可解释性来自同一个机制**。

**成绩链**：拼接 0.829 → crossattn 0.845 → **tokenattn 0.851** →（3 seed）0.866±0.018 →（H100 7 seed）0.874±0.033 →（7-seed 集成）**0.918**。

> 📌 **可带走的能力（本项目最重要的一条方法论）**：**多模态融合的通用正确姿势 = 把异构输入 token 化 → 放进同一注意力空间 → 让它们自由交互**。这不是论文作者的独创，而是 ViT、CLIP、多模态 LLM 的共同套路——你等于在"序列×特征"这个小战场上重新发现了一遍它。以及：**"加注意力"本身不保证有效（gated 退步），要看交互是否让信息真正流通**。

### 2.8 数据增强 / 去趋势残差化 —— 导师方向，为什么在此无效

**背景**：导师给了 Wen et al.《Time Series Data Augmentation for Deep Learning: A Survey》，其中异常检测实证 U-Net F1 从 0.403 → 0.693（先分解 +0.26、再增强 +0.03），是综述里最硬的正结果。按此做了两条路：

**P1a 正类条件增强**（只对 642 条故障做 jitter/scaling/magnitude_warp/time_warp/window_slice，扩到 K=4/K=8 倍）：
| 配置 | PR-AUC | 同 seed 基线 | 变化 |
|------|--------|-------------|------|
| K=4, seed42 | 0.8151 | 0.9158 | **−0.101** |
| K=8, seed42 | 0.8466 | 0.9158 | −0.069 |
| K=4, seed123 | 0.8467 | 0.8727 | −0.026 |

**P1b 去趋势残差化**（滑动均值窗口 W=10/20 去趋势，让模型学"残差"）：
| 配置 | PR-AUC | 同 seed 基线 | 变化 |
|------|--------|-------------|------|
| W=20, seed42 | 0.8622 | 0.9158 | −0.054 |
| W=10, seed42 | 0.8147 | 0.9158 | −0.101 |
| W=20, seed123 | 0.8668 | 0.8727 | −0.006 |

**为什么增强在 U-Net 上有效、在我们这里无效（机制层面，这是教科书级对比）**：
1. **增强的对象错了**：综述场景是"模型主要从序列学"（U-Net 端到端吃原始信号）；而本项目消融证明**判别主力是 62 维特征分支**（移除基础统计量 → 0.916 崩到 0.303）。增强只扰动 6 通道序列分支 = **给次要通道化妆**，主力通道纹丝不动。
2. **增强破坏了故障签名**：jitter/scaling 这类噪声扰动会模糊"超温/SOC 跳变/功率未衰减"这些本来就微弱的异常签名——等于把正样本往错误方向扩，模型学到更糊的故障概念。
3. **训练动态被改变**：正样本扩 K 倍后，`pos_weight` 从 20 降到 5~3，优化轨迹变了——增强的"量变"没有带来"质变"，反而打乱了原本调好的不平衡训练配置。
4. **残差化同理**：去趋势把"功率是否衰减"这个**最强判别信号**（故障=功率未衰减就中断）直接抹掉了！等于把答案从卷面上擦掉再让学生做题。

> 📌 **可带走的能力（这是"负结果"的顶级教学价值）**：**增强只在"模型主要从被增强的那路输入学习"时有效**。做增强前先问：① 增强对象是不是判别主力？② 增强操作会不会破坏你要检测的"微弱签名"？③ 增强改变了类别比例/损失权重，训练配置要不要跟着调？——这三点你在论文局限性里如实写成了"已探索并排除"，审稿人很难用"你没试过增强"来攻击你。

### 2.9 特征验证（P0c）—— 一个"反向确认"实验

**做了什么**：给 62 维特征**再加 4 类新特征**（二阶差分、频域谱、变点、滞后相关）喂 LightGBM，看还有没有"油水"：
| 特征集 | PR-AUC | 变化 |
|--------|--------|------|
| 62 维基线 | 0.8684 | — |
| +二阶差分 | 0.8777 | +0.009 |
| +频域谱 | 0.8744 | +0.006 |
| +变点 | 0.8611 | −0.007 |
| +滞后相关 | 0.8297 | **−0.039** |
| +全部 ABCD | 0.8419 | −0.027（过拟合） |

**结论**：62 维统计特征**已基本榨干时序信号**——新特征最多 +0.009，远不到质变门槛，加多了反而过拟合。

**这个实验的隐藏价值**：它用一个"1 秒训练"的 LightGBM 反向证明了——**本项目的信息上限就在这 62 维特征附近**，任何"想从数据里再挖新信息"的路线（自监督预训练、更大模型、更多特征）预期收益都低。于是决策收敛：**别再挖信息了，把现有的信息用更好的方式用起来**（融合 + 集成）——这正是后来成功路线的"刹车片"：它阻止了团队在错误方向上继续烧算力。

> 📌 **可带走的能力**：**用 1 秒的基线做"信息饱和测试"**——当你犹豫"要不要上大模型/预训练挖新信息"时，先给当前特征集加料喂 GBDT，如果加不动，说明瓶颈不在"信息量"而在"信息利用方式"。这个判断能在项目早期省下几周 GPU 时间。

### 2.10 seed 集成与统计检验 —— 最后的 +0.045，几乎是"白捡"的

**现象**：Token-Attn 单 seed PR-AUC 波动极大——H100 上 7 个 seed 是 **0.819 / 0.853 / 0.857 / 0.873 / 0.876 / 0.916 / 0.921**（均值 0.874±0.033）。同一个模型，最好的 seed 比最差的 seed 高 0.10！而 Mac 上只跑了 3 个 seed（0.851/0.886/0.861），恰好都落在中低值，**严重低估了模型的真实上限**。此外 MPS 与 CUDA 浮点数值不同，导致相同 seed 落在分布的不同位置。

**seed 集成做了什么**：把 7 个模型的**输出概率做平均**（而非投票），再算指标：
| 口径 | PR-AUC |
|------|--------|
| 单 seed 均值 | 0.8736±0.0332 |
| **7-seed 概率集成** | **0.9184**（+0.045） |
| AUC | 0.9903 |
| F1(th=0.5) | 0.7946 |

**为什么这么有效（机制层面）**：
1. **强不平衡 + 小正样本 → 每个 seed 的初始化/批次顺序不同 → 学到的决策边界方差巨大**。这是"小样本强不平衡下深度模型"的结构性宿命，不是调参能消除的。
2. 概率平均 = 模型集成，多个"看问题的角度"（不同随机起点）平均后，**个别 seed 的偏见过拟合被抵消，方差下降，期望性能上升**——尤其对 PR-AUC 这种对排序敏感的指标，多个概率的均值排序比单个更稳。
3. **统计检验把"看起来高"变成"统计上高"**：配对 bootstrap（PR-AUC 差，B=10000）diff=+0.049，95%CI=[+0.006,+0.100]，**p=0.026**——对 LightGBM(0.868) 边际显著；DeLong(AUC) p=0.321 不显著（AUC 被多数类主导，对少数类不敏感，符合预期）。**这给了论文"显著超越 GBDT"的底气，且表述诚实（边际显著、仅 PR-AUC、仅对集成成立）。**

> 📌 **可带走的能力（报告纪律 = 免费涨分）**：
> ① 深度模型在**小样本/强不平衡**任务上，**必须报告多 seed 分布**（均值±std + 范围），否则你报的"单次 0.85"可能只是运气；
> ② **seed 集成是论文标准动作**，几乎总是 +0.01~0.05，代价只是训练 K 次（H100 上单次 160 秒，7 次也就 20 分钟）；
> ③ 任何"我的模型比基线好"的主张，配一个 **bootstrap/DeLong p 值**才算数；
> ④ **别忘了硬件口径**：Mac(MPS) 与 H100(CUDA) 的浮点实现不同会让 seed 落在不同位置——跨机器对比要注明环境。

---

## Part 3｜复盘：最终成功了，为什么一开始没想到？

这是你最想要的部分。诚实拆解，"没想到"至少有四层原因，从最外到最内：

### 3.1 第一层：外部约束制造了结构性弯路（导师批注 ≠ 技术最优）

时间线回到最早期：XGBoost + 57 维特征 + TreeSHAP，PR-AUC **0.893**，性能全场最高。导师 5 条批注要求"换端到端深度模型、换神经网络可解释方法"——于是把精心设计的 57 维特征**全部扔掉**，改纯 Bi-LSTM 吃 6 通道原始曲线，PR-AUC 从 0.893 直接跌到 **0.351**。

**为什么没想到**：因为把"导师要深度模型"错误地翻译成了"导师要抛弃特征"。导师真正反对的是"XGBoost 是黑盒、不够 modern"，而不是"手工特征没用"。**正确的翻译应该是"深度模型 + 特征融合"**（既能交差，又不丢信息）——这个"翻译错误"让团队在 0.35 的泥潭里挣扎了相当长一段时间。

> 教训：**约束要翻译成技术方案，而不是照字面执行**。"用深度模型"和"扔掉特征"是两件事，前者是导师要的，后者是团队自己加的戏。任何来自外部的技术方向意见，先问"它真正反对的是什么、它允许的解法空间有多大"。

### 3.2 第二层：套文献范式，而不是先解剖任务（重建式/域自适应的失败根源）

异常检测领域的主流文献范式是**无监督重建式**（VAE/USAD/Anomaly Transformer）和**域自适应**——这俩是"政治正确"的方向，于是都试了：LSTM-AE 0.14、CORAL ≈0。

**为什么没想到先做任务解剖**：如果早一点做那张"故障指纹"描述性分析（图 1：故障样本 vs 正常样本的统计量对比——末端 SOC 低 15.6pp、末端功率高 26.9kW、最低电流高 69.4A），就会**一眼看出**：① 异常在原始曲线上非常微弱、但在统计量上极其显著（差别巨大）→ 重建式必死；② 判别信号是"物理统计规律"而不是"时序模式" → 特征路线是主场。**一张描述性统计图，价值抵得上两个范式的实验**。事后消融（移除基础统计量 → 0.916 崩到 0.303）完全印证了这一点。

> 教训：**选范式前先解剖任务**——有标签吗？异常在原始信号上明显吗？信息在过程还是统计里？跨域漂移是输入级还是标签级？这四问（对应 Part 1 的表）能在动手前淘汰掉一半的候选方向。**文献的正确 ≠ 你任务上的正确**，每个范式都带着自己的前提假设进场。

### 3.3 第三层：被"数字假象"骗了两次（工程纪律问题）

- **断点续训 bug**：0.748 被当成"深度天花板"，真实是 0.810+——一个优化器状态没存的小 bug，让团队在"深度打不过 GBDT"的错误结论下思考了很久；
- **MPS 3-seed 偶然性**：Mac 只跑 3 个 seed（0.851/0.886/0.861），恰好都落在中低值 → 得出"与 GBDT 打平(0.866 vs 0.868)"的保守结论；H100 跑 7 个 seed 才发现单 seed 能到 0.92、集成能到 0.9184。

**为什么没想到**：因为**"跑一个数就信一个数"是新手直觉**——深度模型是有随机性的，单次实验的数字是"一次抽样"而不是"模型真实水平"。真正的模型水平是一个**分布**。

> 教训：**数字会骗人，纪律才能防骗**——完整训练（不续训）、多 seed（≥5）、集成报告、统计检验、硬件口径标注。这五条不是"加分项"，是"可信度的地基"。讽刺的是，这两次"数字事故"如果早避免，0.918 的结论可能提前两周出现——**提升报告纪律约等于白捡分数**。

### 3.4 第四层（最核心）：默认"更强模型"是解药，其实"融合方式"才是杠杆

团队一路的直觉是"模型不够强"——于是试更强的编码器（iTransformer/PatchTST/FT/Dual，全灭）、更大的池化（多尺度，无效）、换损失（Focal，卡死）……**全部在"让单个模型更强"这个方向上使劲**。

**真正的解药在另一个维度**：问题不是"模型不够强"，而是"**特征和序列没有以正确的方式结合**"。Token-Attn 的胜利不是因为它比 PatchTST 强，而是因为它**改变了信息的组织方式**——把异构输入 token 化、放进同一注意力空间自由交互。这一下同时解决三件事：序列信息被用上了（不再是噪声）、特征主导得到保留、注意力本身成为可解释证据。

**为什么一开始没想到**：因为"多模态融合的正确姿势 = token 化 + 自注意力"这个认知，是 ViT/CLIP/多模态 LLM 界的通用智慧，而做异常检测时人容易局限在"时序模型"的既有框架里（LSTM/Transformer 编码器/重建误差），**没有意识到本项目本质上是"序列×特征"的多模态融合问题**。一旦换框架看它，答案几乎是现成的。

> 教训：**卡住时先换"问题框架"而不是换"模型"**。当你在"时序异常检测"的框架里所有路都堵死时，试着问"这题还能被看成什么题？"——本项目的答案是"一个序列+表格的多模态融合题 + 一个强不平衡小样本题"。框架一换，教科书答案（token 化融合、seed 集成、统计检验）全都对号入座了。

### 3.5 三条最值钱的复盘结论

1. **这题的信息在特征里，不在曲线里**——所有"纯吃原始信号"的尝试（Bi-LSTM/iTransformer/PatchTST/增强）都败在这条；所有"让特征当主力、序列做补充"的尝试都赢在这条。一张故障指纹对比图本可以在第一天告诉你这件事。
2. **深度模型的敌人不是 GBDT，是"高方差 + 信息密度低"**——用 seed 集成解决前者（+0.045），用 token 化融合解决后者（0.351→0.851）。
3. **你的论文卖点（可解释性）不是模型的附加品，而是模型设计的产物**——注意力融合让"性能"和"可解释"来自同一机制，这是 GBDT 无论多强都给不出的东西，也是它能当主模型、GBDT 只能当参照的真正原因。

---

## Part 4｜带走的能力：一套可复用的任务选型方法论

### 4.1 新任务解剖 8 问（动手前 30 分钟做完）

| # | 问题 | 如果答案是…… | 倾向的方向 |
|---|------|--------------|-----------|
| 1 | 有标签吗？标签多稀有？ | 无标签 / 正样本 <1% | 无监督重建式 / 异常分数路线（并做好心理预期） |
| 2 | 异常在原始信号上"肉眼可见"吗？ | 是结构突变 → 重建式可行；是微弱统计偏移 → 重建式必死，监督+特征路线 |
| 3 | 判别信息在"过程"还是"统计"里？ | 过程（语音/动作）→ LSTM/时序模型；统计（状态判断）→ 特征工程+分类器 |
| 4 | 训练/测试分布一致吗？ | 一致 → 大胆端到端；不一致 → 域无关特征 or 域自适应（先分清输入漂移还是标签漂移） |
| 5 | 样本量多大？ | <1 万 → 树模型/小模型+集成；>10 万 → 深度模型主场 |
| 6 | 可解释性刚需吗？ | 是 → 优先注意力/可解释结构，别选纯黑盒当主模型 |
| 7 | 有没有领域知识可做成特征？ | 有 → 融合路线（特征 token + 数据 token），别扔掉 |
| 8 | 算力/时间预算？ | 紧 → GBDT 先跑天花板；松 → 深度 + 多 seed 集成 |

### 4.2 决策路径（从解剖结果到方案）

```
任务解剖(8问)
   │
   ├─ 信息在特征里 + 有标签 + 可解释刚需  → 树模型跑天花板(1秒) → 深度×特征 token 融合(注意力交互) → seed 集成 → 统计检验
   ├─ 信息在过程里 + 有标签               → LSTM/TCN 编码 → 分类头 → (可解释: GradCAM/注意力)
   ├─ 无标签 + 异常结构性明显            → 重建式 AE/VAE (USAD/Anomaly Transformer)
   └─ 无标签 + 异常微弱                  → 难。先做统计特征 + 无监督异常分(IsolationForest)，并诚实告诉需求方上限
```

**融合类任务的设计黄金律**（本项目验证）：异构输入 **token 化 → 统一注意力空间 → 自由交互 → 用注意力当可解释证据**。适用：序列×表格、图像×文本、多传感器……凡是"两个信息源要互相参考"的任务。

### 4.3 报告纪律五条（可信度的地基）

1. **完整训练**，不续训（要续训就存优化器状态）；
2. 深度模型报 **≥5 seed 的均值±std + 范围**，不报单次；
3. 主结果用 **seed 概率集成**口径（几乎总是更好）;
4. 任何"超越基线"主张配 **bootstrap/DeLong p 值**，并如实标注显著性边界；
5. 跨机器（MPS/CPU/CUDA）比较**注明硬件与浮点口径**，GBDT 类模型可做逐位一致性校验。

### 4.4 反向清单：文献热门但本项目实测无效（= 你的"排除证据库"）

| 方向 | 实测 | 一句话原因 |
|------|------|-----------|
| 重建式（LSTM-AE/USAD/Anomaly Transformer 同类） | 0.14 | 微弱异常被平滑 + 跨站漂移 + 浪费标签 |
| 域自适应（CORAL） | ≈0 收益 | 新站点是标签漂移不是纯输入漂移，且二阶对齐太浅 |
| 换更强序列编码器（iTransformer/PatchTST/FT/Dual） | 全灭 | 燃料（信息入口）没解决，换引擎无用 |
| 简单拼接融合 | 0.829（低于交互融合） | 无跨模态交互通道 |
| Gated 门控 | 0.796 退步 | 交互太浅，"加门控≠有效" |
| 多尺度池化 / Focal Loss | 0.806 / 卡死 | 堆料无效；Focal 在 MPS 死锁 |
| 简单时域增强 / 去趋势残差化 | −0.10~−0.03 | 增强对象非判别主力 + 破坏微弱签名 |
| 再加手工特征（差分/频域/变点/滞后） | ≤+0.009 | 62 维已饱和，信息在"用法"不在"量" |

> 这套反向清单在答辩/审稿里的价值：任何 reviewer 说"你为什么不试 XXX"，你都能给出**有数字的否定答案**——这比"没试过"有说服力一个数量级。

---

## Part 5｜延伸学习路线图（如果想继续深挖）

按兴趣选一条，不用全学：

- **想做时序异常检测本身**：读 MTAD-GAT（图注意力）、GDN（传感器关系图）、Anomaly Transformer（关联差异）、OmniAnomaly/USAD（重建式），对照本项目的失败原因看它们各自的前提假设。
- **想搞懂 Transformer 家族**：从"Attention is All You Need"→ ViT（token 化思想）→ CLIP（跨模态对齐）→ iTransformer/PatchTST（时序适配）→ 本项目 Token-Attn 是这条线的"迷你版应用"。
- **想搞懂融合/多模态**：CLIP、Florence、多模态 LLM 的输入 token 化设计——你的 Token-Attn 是同一思想在小数据上的复现。
- **想搞懂 XAI**：SHAP（博弈论）、Integrated Gradients（公理化）、TreeSHAP、注意力争议（"Attention is not Explanation"）+ 本文的交叉印证法（注意力+置换+消融三路一致 = 缓解争议的标准姿势）。
- **想搞懂统计检验/实验方法**：bootstrap 配对检验、DeLong 检验、seed 方差分析——这是"AI 实验报告可信度"的核心手艺，很多论文翻车就在这。
- **想搞懂强不平衡学习**：加权 CE、focal loss（及其在 MPS 的坑）、阈值校准、seed 集成、数据增强的边界——本项目每个都踩过。

**建议的学习闭环**：每读一个新模型论文，套用本笔记的"四问"——它是什么/天生擅长什么/前提假设是什么/如果放在我们的充电站数据上会怎样？你会发现大部分模型的生死，早在动手前就由"前提假设是否成立"决定了。

---

## 附 A｜成绩总表（全部真实数字，可对照论文表 1）

| 方法 | PR-AUC | AUC | F1(th=0.5) | 环境/口径 |
|------|--------|-----|-----------|----------|
| 纯 Bi-LSTM | 0.351 | 0.908 | 0.359 | 端到端 6 通道 |
| LightGBM 62 维 | 0.868 | 0.987 | 0.480 | Mac/H100 一致 |
| XGBoost 62 维 | 0.861 / 0.887 | — | 0.486 | Mac / H100 |
| XGBoost 57 维（旧，owner1-4） | 0.893 | — | — | 口径不同 |
| LSTM-AE | 0.140 | 0.853 | — | 重建式 |
| CORAL(lam=0.5) | 0.825 | — | — | 域自适应 |
| BiLSTM+特征拼接 | 0.829 | 0.967 | 0.769 | bug 修正后 |
| iTransformer / PatchTST / FT-Transformer / Dual | 0.592 / 0.744 / 0.716 / 0.275 | — | — | 早期早停协议 |
| 多尺度池化 | 0.806 | 0.972 | 0.739 | C1 |
| Gated | 0.796 | 0.961 | 0.693 | C2 |
| Cross-Attn | 0.845 | 0.977 | 0.753 | C2 |
| Token-Attn(seed42) | 0.851(→0.916 H100) | — | — | C2 |
| Token-Attn 单 seed 均值 | 0.874±0.033 | — | — | H100, 7 seed |
| **Token-Attn 7-seed 集成** | **0.918** | **0.990** | **0.795** | 🏆 论文主口径 |
| 数据增强 K=4/K=8 | 0.815 / 0.847 | — | — | P1a 负结果 |
| 残差化 W20/W10 | 0.862 / 0.815 | — | — | P1b 负结果 |

## 附 B｜本文数字来源文件索引

- 实验过程与踩坑：`实验探索全记录_方法与复现手册.md`
- H100 夜跑全自动实验（7-seed/集成/统计/增强/残差/特征验证）：`AUTORUN_实验报告.md`
- 项目早期背景与导师批注史：`HANDOFF.md`
- 论文终稿（成绩与叙事口径）：`paper/paper_draft.md`
- 可解释性基础概念（指标/阈值/SHAP/Bi-LSTM 通俗版）：`论文核心概念通俗解释.md`
