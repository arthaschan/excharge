# excharge 论文复现环境依赖（R2 主线，仅含论文用到的包）
# 安装: pip install -r requirements.txt
#
# 注意 torch 需按 CUDA 版本单独安装（勿用本文件直接装 torch），例如 CUDA 12.4：
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# 本项目实测环境（见 README「环境初始化」）：
#   python 3.11 / torch 2.6.0+cu124 / numpy 2.4.6 / pandas 3.0.5
#   scikit-learn 1.9.0 / xgboost 3.2.0 / lightgbm 4.7.0 / pyarrow 25.0.1
#   matplotlib 3.11.1 / scipy 1.17.1 / openpyxl 3.x

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
pyarrow>=14.0
matplotlib>=3.7
scipy>=1.10
openpyxl>=3.1
