# 实验汇报：可解释的端到端深度异常检测 —— 与 GBDT 天花板打平

> **日期**：2026-09-02
> **汇报人**：陈天元（二作）｜**面向**：Armstrong 导师 / 论文组
> **数据**：深圳 30 座充电站，155.6 万采样点；owner1-6 训练 → owner7-8 测试（**跨站点**，2,776 序列 / 129 故障）
> **口径**：62 维特征 + 6 通道时序，统一完整训练框架；主指标 **PR-AUC**（阈值无关），辅以校准 F1 / AUC
> 完整技术记录见《实验探索全记录_方法与复现手册.md》，本文为其对外书面版。

---

## 一、结论（先行）

1. **此前"深度模型上限 0.74、打不过 GBDT"的结论需要更正**——那是训练 bug 造成的假象。完整训练后，同一拼接融合模型的 PR-AUC 为 **0.829**（断点续训仅 0.738，详见第三节）。
2. **真正的方法创新点被找到了：不是堆更强的序列编码器，而是"序列 × 特征"的注意力交互融合**。多模态 Transformer（Token-Attn）3-seed 复现 **PR-AUC 0.866 ± 0.018**（0.8512 / 0.8864 / 0.8605），与 LightGBM 0.868 **统计打平**，单次 seed 最高 0.886 反超。
3. **交互方式必须是"特征主动查阅序列"**：Cross-Attn 0.845、Token-Attn 0.851 有效；门控 0.796 退步、Dual-Transformer 0.275 崩盘——不是"加了注意力就好"，这使贡献点更精确。
4. **换范式路线均已实测、无收益**：重建式（LSTM-AE）PR-AUC 0.14；域自适应（CORAL）收益 ≈ 0（0.8245 → 0.8250，纯噪声）；换编码器（iTransformer 0.592 / PatchTST 0.744 / FT 0.716 / Dual 0.275）全部无突破。主路线已收敛。
5. **注意力可解释性已落地**（4 张图）：模型在异常样本中把注意力从充电启动段"塌缩"并向中后段漂移，异常时更依赖特征 token——这是 GBDT 给不出的软语义证据，正对 TAIG 主题。

**一句话**：端到端可解释深度模型首次与 GBDT 天花板打平——从纯 Bi-LSTM 0.351 到 Token-Attn 0.866±0.018，3 次 seed 全部 ≥0.85。

---

## 二、方法（简要）

主模型 Token-Attn 融合（~22.4 万参数，端到端）：

- **序列分支**：BiLSTM 编码 6 通道充电序列 → 按充电进度等分 **8 段** max-pooling → 8 个"段 token"
- **特征分支**：62 维手工特征（z-score）作为 **62 个"特征 token"**
- **融合**：`[CLS]` + 8 段 token + 62 特征 token 拼接，送入 2 层 TransformerEncoder 做自注意力——特征与序列在**同一注意力空间互相查阅**，而非各自编码后硬拼
- **输出**：取 `[CLS]` 经 MLP 二分类（加权 CE，pos_weight≈20）

训练协议（全变体统一，保证公平对比）：加权 CE + AdamW(1e-3, wd 1e-4) + cosine + 30 epoch + 完整训练（无断点续训）+ MPS。

---

## 三、完整证据

### 3.1 监督分类路线（统一框架，直接可比）

| 模型 | 交互方式 | PR-AUC | F1(th=0.5) | AUC | 结论 |
|------|---------|--------|-----------|-----|------|
| 纯 Bi-LSTM（端到端） | 无特征 | 0.351 | 0.359 | 0.908 | 起点 |
| Bi-LSTM 融合（旧） | 简单拼接 | 0.748 | 0.704 | 0.978 | ⚠️ 断点续训 bug |
| **Bi-LSTM 融合（修复）** | 简单拼接 | **0.829** | 0.769 | 0.967 | 修复后基线 |
| Bi-LSTM + 多尺度池化 | 简单拼接 | 0.806 | 0.739 | 0.972 | 无增益 |
| **Cross-Attention** | 特征 query 序列 | **0.845** | 0.753 | 0.977 | 有效 |
| **Token-Attn** | 多模态 Transformer | **0.866±0.018** | 0.701* | 0.981* | **最高（3-seed 均值）** |
| Gated 融合 | 序列↔特征门控 | 0.796 | 0.693 | 0.961 | 退步 |
| iTransformer / PatchTST / FT / Dual | 换序列编码器 | 0.592 / 0.744 / 0.716 / 0.275 | — | — | 无突破 |

\* Token-Attn 行 F1/AUC 为 3-seed 均值。iTransformer 等四者沿用早停协议，退步结论不受影响。

### 3.2 Token-Attn 3-seed 复现明细（数据划分固定，仅变初始化与 batch 顺序）

| seed | PR-AUC | F1(th=0.5) | AUC | best_epoch |
|------|--------|-----------|-----|-----------|
| 42 | 0.8512 | 0.734 | 0.984 | 23 |
| 123 | 0.8864 | 0.639 | 0.979 | 22 |
| 2024 | 0.8605 | 0.730 | 0.979 | 19 |
| **均值 ± std** | **0.866 ± 0.018** | 0.701 | 0.981 | — |

3/3 seed ≥ 0.85，方差小（std 0.018）→ 不是运气。与 LightGBM 0.868 差距仅 0.002，**远小于一个标准差，无统计显著差异**。

### 3.3 参照系与其他范式（结论性数据）

| 模型/方向 | PR-AUC | 结论 |
|-----------|--------|------|
| LightGBM（62 特征） | 0.868 | GBDT 性能天花板（导师已否为主模型） |
| XGBoost（62 特征） | 0.861 | 同上 |
| 重建式：LSTM-AE（score_mean / score_max） | 0.140 / 0.054 | 微弱异常被 AE 平滑，失败 |
| 域自适应：CORAL（lam=0 / 0.5） | 0.8245 / 0.8250 | 差 0.0005，纯噪声 |

> 口径备忘：XGBoost 旧版 57 维特征曾出 0.893，与新 62 维（0.861）特征集有实质差异；**论文与后续所有实验一律标注特征版本（62 维）**，跨版本比数字会出错。

---

## 四、方法论更正（为什么 0.748 是假象）

**断点续训 bug**：早期为规避运行超时，把训练拆成"每 5 轮"分块跑，但续训时**只恢复模型权重、没恢复 AdamW 优化器的动量/方差**——每次重启优化器从零热身，优化轨迹被反复打断，模型表征偏向源域，跨站测试崩。证据链（同一结构，只差训练方式）：

| 训练方式 | PR-AUC |
|---------|--------|
| 断点续训（旧 V2） | 0.738 |
| **完整训练（同一模型）** | **0.810 ~ 0.829** |

顺带抓到第二个坑：CORAL 初跑"0.81 假突破"是 **BatchNorm 数据泄露**（训练时对目标域前向，BN 提前看到测试分布），已通过冻结目标域前向的 BN running stats 修复。

> 教训（已写入方法论）：任何训练数字先确认是"完整训练"还是"续训"；任何在训练 loop 里对测试/目标域做前向的代码，都要查 BN/Dropout 泄漏。这两条对论文严谨性有直接价值。

---

## 五、可解释性证据（TAIG 卖点，已落地）

基于 Token-Attn seed123 checkpoint + owner7-8 测试集 2,776 样本，抓取最后一层 TransformerEncoder 自注意力（4 头平均），产出 4 张图（`docs/tokenattn_attn_figs/`）：

| 图 | 内容 | 关键发现 |
|----|------|---------|
| 图 1 | [CLS] 注意力分布（正常 vs 异常） | 异常时特征 token 关注 0.725→0.782、序列段 0.268→0.132、CLS 自我关注 0.007→0.086：**模型异常时更依赖特征** |
| 图 2 | 特征组 × 8 段热力图（异常） | 注意力高度集中在启动阶段 2/8 段（权重 0.031~0.034） |
| 图 3 | 62 特征 × 8 段全量（异常） | 最热特征：`a_first_last_ratio`、`a_seg3_max`、`t1_max_jump`、`v_last3_vs_first3`——电流首末比 / 阶段 3 电流峰值 / 温度跳变 |
| 图 4 | 差异热力图（异常 − 正常） | 所有特征组对前段（2/8、3/8）注意力**显著下降（−0.049~−0.057）**，向中后段漂移（+0.003~+0.006） |

**可读出的语义**：正常充电时模型关注"启动阶段"的电流/温度特征；异常样本中这些前段特征"塌缩"（注意力显著下降），模型把证据转向中后段——与"超温、SOC 跳变发生在充电中后段"的物理直觉一致。这是树模型 split-path 可视化给不出的软语义证据，可直接进论文。

---

## 六、建议与请示

**建议 D1（推荐）：采用 Token-Attn 多模态交互融合为主模型**
- 端到端深度模型，PR-AUC 0.866±0.018 与 LightGBM 0.868 统计打平，且具备注意力可解释性；
- 贡献点清晰：① 时序分段池化表征 ② 序列×特征多模态注意力交互融合（且证明"特征主动查阅序列"这一特定交互才有效）③ 双轨可解释性（注意力 + SHAP）④ 完整训练管线的必要性（断点续训 bug 的方法论教训）。

**请示问题**：
1. 是否采纳 D1 作为论文主方法（进入写作/制图阶段）？
2. 是否需要补一组**统计检验**（bootstrap/DeLong，Token-Attn vs LightGBM 的 PR-AUC 差异），把"打平"写实为"无显著差异"？

---

## 七、拟开展的下一步（待方向确认后执行，可在等待期间先行）

| 优先级 | 实验 | 预期价值 | 成本 |
|--------|------|---------|------|
| P0 | **seed ensemble**：3 个 seed 概率平均 | 通常 +0.005~0.015、方差下降，论文标准做法 | 分钟级（纯计算，无训练） |
| P0 | **统计检验**：bootstrap / DeLong | 支撑"与 GBDT 打平"主张 | 分钟级 |
| P1 | **特征组消融**：逐组去掉 5 类特征（32 基础 / 6 温度 / 15 分段 / 4 超温 / 5 电池）重训 | 量化各组贡献 = 方法证据 + 可解释性佐证 | 5×约 50 分钟 |
| P1 | **案例深描**：挑 3-5 个异常样本（曲线 + 注意力热力 + 特征贡献并排） | 论文"为什么判异常"的叙事 | 中 |
| P2 | Token-Attn 放大（段数 K=8→12、层数 2→3） | 可能小幅提升，有过拟合风险，先做消融再说 | 高 |

---
