# voltaic 数据真实性审计 + 充电中早期预警方向综述判定

> 交付日期：2026-09-03 | 对应你三个问题：
> ① voltaic 测试任务哪些数据是 AI 伪造的（伪造部分不能作期刊依据）；
> ② "充电过程在线早期预警"方向是否真有价值、是否已被别人做过；
> ③ 如果有价值，怎么深挖。
>
> 审计基于 `/Users/arthas/git/voltaic/` 代码与数据实读；综述基于 7 轮网络检索（英文 5 + 中文 2）。
> 定位：决策文档，不含新实验。

---

## 第 1 部分：voltaic 数据审计 —— 哪些是假的、影响谁、期刊红线

### 1.1 结论先行

| # | 结论 | 等级 |
|---|---|---|
| 1 | voltaic 28 列里 **17 列是 AI/规则确定性伪造**（温度=正弦公式、降雨=月份规则、行政区=轮播、NEV=固定常量、网格设施=0/复制） | 🔴 不可作期刊依据 |
| 2 | **功率曲线本身 + 真值（GT）100% 真实**——来自源 xlsx 的真实测量与真实聚合，伪造只发生在"语义/环境列" | ✅ 可作依据 |
| 3 | **我方模型（LightGBM/PatchTST/TimesNet）只吃真实列**（`power_w/connector_id/report_time`），完全未触碰伪造列 | ✅ 无污染 |
| 4 | **教授模型被污染**：GRU 把伪造的 `temperature_c` 当输入特征；DEMMFL 喂 `temperature_c`+`rainfall_mm`；Slot-Median 干净（只用功率+工作日） | ⚠️ 涉及其性能数字需重跑或用真实列复算 |
| 5 | **对 excharge 期刊论文的影响 = 零污染面**：期刊用 excharge 自己的真实数据；只要不引用 voltaic 那 17 列和依赖它们的模型数字即可 | ✅ 主线安全 |

### 1.2 伪造清单（代码级证据，convert_full.py `build_28cols()` L122-162 + INPUT_HEADERS L54-62）

| 列名 | 伪造方式 | 证据行 |
|---|---|---|
| `temperature_c` | 温度 = 20 + 8·sin(2π·月/12) + 3·sin(2π·时/24)（纯正弦，无真实测量） | L128 |
| `rainfall_mm` | 月份 4-8 月 = 5.0，其余 = 0（规则，无真实降雨） | L129 |
| `district_nev_penetration_rate` | 固定常量 0.666012 | L145 |
| `district_ev_sales` | 固定常量 16605 | L146 |
| `district_bev_ratio` | 固定常量 0.435592 | L147 |
| `district_operating_ev_ratio` | 固定常量 0.055345 | L148 |
| `district_name` | 深圳 10 区名**轮播**（与真实位置无关） | L51-52, L153 |
| `grid_id` | 合成编号 `L2FT01-FTJxxx` | L282 |
| `grid_area_km2` | 0.3 + (gi % 7)·0.12（轮播） | L284 |
| `grid_total_charging_facilities` | = 该 case 真实桩数（语义张冠李戴） | L154 |
| `grid_slow_charging_facilities` | = 同上 | L155 |
| `grid_public_slow_charging_stations` | = 真实站数 | L160 |
| `grid_fast_/super_*/public_fast_*`（5 列） | 硬编码 0 | L156-159 |
| `has_major_event` | 硬编码 0 | L150 |

> **代码自述**：convert_full.py 头注释 L17"确定性伪造(17)"；manifest notes 同样写明"真实列5+派生4+日历2+确定性伪造17"。
> 派生 4 列（`station_id/connector_id/operator_id/connector_status`）与日历 2 列（`is_workday/is_major_holiday`）**不算伪造**——它们是 transaction_id 前缀/功率正负/公历日期的确定性推导，等价于脱敏 ID 与日历特征。

### 1.3 哪些列是干净的（期刊可用）

| 类别 | 列 | 说明 |
|---|---|---|
| ✅ 真实测量(5) | `current_a` `voltage_v` `power_w` `report_time` `date` | 源 `charginga/chargingv/out_power`×1000，窗口平均 |
| ✅ 真实派生(4) | `station_id` `connector_id` `connector_status` `operator_id` | 确定性、无测量值可造假空间 |
| ✅ 日历派生(2) | `is_workday` `is_major_holiday` | 由真实日期 + 中国节假日表推导 |

**GT 真实**：`write_ground_truth` L165-190 是对真实源采样的聚合，无 AI 参与。

### 1.4 对 voltaic 报告（7模型对比报告.md）数字的影响

| 报告里的行 | 输入是否含伪造列 | 处置建议 |
|---|---|---|
| All-Zero / Slot-Median | 否（Slot-Median 仅 power+is_workday） | ✅ 可直接引用 |
| LightGBM / PatchTST / TimesNet（我方） | 否（只用真实功率序列） | ✅ 可直接引用 |
| GRU（教授） | **是**——特征含 `temperature_c`（run_merged_benchmark.py L105, L150） | ⚠️ 期刊引用前用"去掉温度列"重跑，或标注为受造数据影响 |
| DEMMFL（教授） | **是**——特征含 `temperature_c`/`rainfall_mm`（professor_model_zoo.py L301-302） | ⚠️ 同上 |

> 好消息：报告的**头名（Slot-Median MAE 27.68）是干净模型**，所以"简单统计方法在稀疏场景最稳"这个核心结论**不因伪造列而动摇**。受污染的只是 GRU/DEMMFL 的排名依据，它们在预测未来 4 天时温度实为"末日常量回填"（L150），实际信息量低——影响程度有限，但**不能原样写进期刊方法描述**。

### 1.5 excharge 期刊论文红线清单

1. ❌ 不引用 voltaic 的 `temperature_c / rainfall_mm / district_* / grid_* / has_major_event` 作特征、作可视化、作数据字典条目。
2. ❌ 不把 voltaic 报告里 GRU/DEMMFL 的原始数字当"教授基线表现"写进期刊；要引用就**用真实列重跑**后的版本。
3. ✅ 可引用：功率预测曲线、GT、我方 3 模型对比、Slot-Median/All-Zero、跨站泛化设定。
4. ✅ excharge 自己的 `data/real/`（8 owner / 31,449 事务 / 6 通道）全程真实，期刊主线数据无虞。
5. ⚠️ 若期刊实验想用 voltaic 的 28-case 多站设定，只取干净 11 列；并如实注明源数据与字段血缘。

---

## 第 2 部分：充电中早期预警方向 —— 综述判定

### 2.1 一句话判定

> **方向价值：确认高（真问题、真市场、顶刊在收）。但"充电过程故障提前预警"这个大标题已被做滥——2024-2026 年密集发表了至少 6+ 篇直接竞争论文（含 Nature Communications 2025）。**
> **真正的空白不在"提前预警"，而在三个窄轴上：① 预警对象 = 会话"异常终止（跳枪）"而非电池过热；② 双谱系分型：启动型(<5min 插枪即断) vs 运行型(充到中段才断)——启动型家族在学术界近乎空白，只存在于行业排障手册；③ 纯桩侧 6 通道 + 前缀最短可预警时刻(EAR) + 跨站泛化(1-6→7-8)。**

### 2.2 检索证据地图（7 轮，2026-09-03）

| 论文/来源 | 年份/级别 | 做了什么 | 与我们重叠点 | 差异点 |
|---|---|---|---|---|
| MA-BiLSTM-MCoAE（Energy, Elsevier） | 2025 | 卷积自编码器+BiLSTM+attention+EWMA，**提前 8.3min 过热 / 11.55min 电压异常**预警 | ⭐ 在线提前预警 | 预测"过热/电压"这类**传感器级物理异常**；用多传感器/电池侧；单会话内连续监测 |
| 车-桩交互数据充电异常检测（CNN-LSTM-Attention） | 近年 | 电压预测残差 → 充电安全预警 | 提前预警 | 需车桩交互双端；预警物理量异常 |
| **Nature Communications 2025**（预测真实 DC 充电曲线与时长） | 2025 | β-VAE 全曲线异常检测（滤脏数据）+ TFT **用部分前缀实时更新**预测完整曲线 | ⭐⭐ 前缀/实时更新 + 异常检测，最接近的强对手 | 异常=数据噪声/异常会话**用于过滤**；不做"会话将异常终止"的二分类预警；无双谱系；无跨站冷启动 |
| Self-Training 充电桩风险评估（IEEE, 9858055） | 2022 | 17,773 条正常/异常终止记录 + 7 统计特征 → 低/中/高风险三分类 | ⭐ 会话级异常终止 | **事后**风险分级（非前缀在线）；7 个聚合统计量；无时间曲线 |
| ST-DAD（ScienceDirect） | 2026 | 时空需求驱动的充电会话异常检测 | 会话级 | 时空聚合需求面，非单会话在线预警 |
| DC 快充站预测性维护（MDPI） | — | 无监督异常 → 预测性维护 | 运维叙事 | 站级，非会话级终止 |
| 自适应深度置信网络充电故障预警（MDPI） | 2021 | DBN 分类故障 | 故障预警 | 分类而非 EAR 预警 |
| 数据驱动充电安全预警（硕士论文, Informer） | 2024 | 短期充电安全预警 | 早期预警 | 单谱系 |
| CNN-BiGRU 充电过程故障预警（ICCEPE） | 2024 | 充电过程故障预警模型 | 早期预警 | 未分终止类型 |
| OCPP + shark-fin/Rp 热失控时间域分析（Sherbrooke 硕论） | — | 热失控前兆识别 | 前缀/早期信号 | 电池热失控，非桩侧终止 |
| 行业排障手册（Siemens VersiCharge 故障表、Tesla/运营商指南、"跳枪"12 因） | 行业 | **"插枪即断=握手/锁扣/授权问题；充几分钟后停=热保护/接触"**——启动型 vs 运行型是行业常识 | ⭐ 印证双谱系真实存在 | **只在手册里，没有数据建模、没有论文** |

### 2.3 诚实评估：什么已被做烂，什么还是空的

**已被做烂（别再当卖点）**：
1. "提前 N 分钟预警过热/电压/热失控" —— MA-BiLSTM-MCoAE 等已到 8-30 分钟，指标已成体系；
2. "重建误差/预测残差/统计控制限" 做在线异常检测 —— 是主流范式；
3. "会话级事后风险分级" —— IEEE 那篇已做（但只有 7 个统计特征、无曲线无前缀）。

**仍是空白（我们的真实窗口）**：
1. **预警目标 = 充电会话"将异常终止"（abort prediction），而非"物理量越限"**——两篇最接近的（NatComm β-VAE 异常过滤；IEEE 事后分级）都没做"充到第几分钟，预测这次会跳枪"；
2. **启动型异常（<5min 插枪即断）的学术建模**：行业手册公认存在（锁扣/握手/CCID/授权），但**没有任何学术工作用桩侧曲线数据把它当一族可预警故障**。我们有全量 31,449 事务里 63.6% 的 <5min 故障 + 真实标签——这是被会议论文口径筛掉的**未发表族群**；
3. **双谱系（启动型 vs 运行型）分型 + 各自"最早可预警前缀时刻"(earliest warnable prefix / EAR)**；
4. **纯桩侧 6 通道（v/a/p/枪温×2/SOC）+ 不用电池/环境外源 + 跨 owner 冷启动**（1-6→7-8）——多数对手依赖 SoC/环境温度/车端信号，我们是"只靠充电站本就有的 OCPP 数据"，部署叙事硬。

### 2.4 期刊故事（要这样写才立得住）

> **"不是又一个提前 N 分钟的传感器预警，而是：充电站只凭自己已有的 6 通道 OCPP 数据，在充电进行到第几分钟就能分型预警'这次充电会跳枪'——首次把运营中人人都见、论文里却没人做的插枪即断(启动型)与中途跳枪(运行型)分开建模，并给出每个家族的最早可预警前缀与跨站(8 个数据方)泛化证据。"**

卖点排序（写稿时按此强调）：
1. 发现 + 建模被论文口径滤掉的**启动型故障族群**（新问题，非新方法）；
2. 双谱系分型的**在线预警提前量曲线 / EAR**（新指标叙事）；
3. 纯桩侧数据 + 跨站冷启动 = 可直接部署到任何 OCPP 平台（场景价值，呼应 V2G 场站安全前置件）。

### 2.5 风险（必须诚实承认）

| 风险 | 说明 | 验证方式（一天内） |
|---|---|---|
| 启动型是否"可学" | 若 <5min 故障与正常短事务曲线无差异 → 双谱系卖点塌 | 启动故障 vs 正常短事务的 GBDT 可分性 + 分布对比 |
| 前缀早期信号强度 | 前 5/10min 若 PR-AUC≈0.5，EAR 故事塌一半 | LightGBM × {5/10/20min 前缀} 快速探测 |
| 与 NatComm/IEEE 的区分度 | 审稿人可能说"β-VAE 已做前缀实时更新" | 明确写清：他们异常=过滤噪声，我们异常=终止预警+双谱系+EAR |

---

## 第 3 部分：判定与深挖路线

### 3.1 汇总判定

| 你的问题 | 判定 |
|---|---|
| ① voltaic 假数据影响 | 17/28 列伪造；**功率+GT 真实，我方模型零污染，Slot-Median 结论可保留**；GRU/DEMMFL 数字需干净列重跑后才能进期刊；excharge 主线数据不受影响 |
| ② 早期预警方向价值 | **有价值（顶刊在收、市场真实），但大标题已拥挤**；空白收缩在"会话异常终止 × 双谱系 × EAR × 纯桩侧 × 跨站" |
| ③ 是否深挖 | **值得深挖，但有条件**：先过"第 0 步"（见下）；过了就是一篇有真差异的期刊论文，过不了就降级为"运行型异常终止的前缀预警"窄故事再评估 |

### 3.2 深挖路线（沿用并强化上一版"方向 A"的行动路线）

```
第 0 步（本机 1 天内，决策门）—— 不过就不立项：
  □ 启动型可学性：启动故障(<30min) vs 正常短事务 的分布/GBDT 可分性
  □ 前缀早期信号：LightGBM × {5/10/20min} 的 PR-AUC 单调性（>随机即过）
  □ owner 分布核验：启动故障在 8 owner 间是否均匀（跨站前提）
  → 全过 → 方向立项（新论文=excharge 期刊版主路线）

第 1 步：双谱系 + 前缀切窗数据集（全量 31,449 事务，不设 ≥30 点下限）
第 2 步：H100 跑 Token-Attn 前缀变体（mask 防偷看未来）+ 基线对比
第 3 步：预警提前量曲线/EAR + 双谱系归因 + 电池条件化 + 跨站泛化
第 4 步：成稿投 Applied Energy / eTransportation / Energy（会议版 TAIG 引用关系写清）
```

### 3.3 一个额外收获（可用于期刊 motivation 与你的专家叙事）

充电事故现实依据（Energy 2025 引言数据）：2022 年一季度中国 >640 起 EV 火灾，**35% 由充电过程引发**；Nature Communications 2025 把"会话噪声/异常"当真实运营痛点。这给"跳枪预警 = 场站安全前置件"提供了扎实的外部动机，也让"voltaic/excharge 同源数据 + V2G 试点"的专家故事更完整。

---

*审计/检索日期 2026-09-03。voltaic 证据文件：`code/convert_full.py`（L122-162 伪造逻辑、L17 自述）、`dataset/README.md`、`code/run_merged_benchmark.py`（L105/L150 教授模型吃温度）、`code/professor_model_zoo.py`（L172/L227/L301-302）。综述检索 7 轮（英文 5 + 中文 2），关键对手论文已列 2.2 表。*
