# excharge 会话上下文（2026-09-02 夜，供下次会话恢复）

> 本文档记录本会话完成的工作、当前状态与待办，供下次新任务快速接手。
> 前置阅读：根目录 `HANDOFF.md`（旧交接）、`docs/汇报_2026-09-02_*` 系列（本会话各阶段汇报）。

---

## 一、项目一句话 + 当前路线

- 项目：可解释充电站异常检测论文（投 TAIG，AI 治理），深圳 30 座充电站 155.6 万采样点。
- **当前路线 = R2（纯 Token-Attn，老师已选定）**：
  - 主模型：Token-Attn 多模态注意力融合（BiLSTM 8 段 token + 62 维特征 token + [CLS] 联合 self-attention）。
  - 性能：7-seed 集成 **PR-AUC=0.918、AUC=0.990、F1(th=0.5)=0.795**；显著超越 LightGBM(0.868, bootstrap p=0.026 边际)、高于 XGBoost(0.887, p=0.143 未达显著)。
  - 可解释性：多视角归因（自注意力 + 置换重要性 + 特征组消融）+ **成因机制分析（§4.5）** + 案例深描 + EU AI Act 合规。
- 已放弃：R1/R3（TabPFN）、"断点续训 bug"失败过程叙事（老师/用户明确要求删除）。

## 二、环境

- 硬件：NVIDIA H100 NVL 96GB / CUDA 12.9 / 96 核 / 377GB。
- Python 环境：`/home/student/arthas/excharge/.venv`（uv 建，python 3.11）。
  - 核心：torch 2.6.0+cu124、numpy 2.4.6、pandas 3.0.5、scikit-learn 1.9.0、xgboost 3.2.0、lightgbm 4.7.0、pyarrow 25.0.1、matplotlib 3.11.1、scipy 1.17.1。
  - 额外已装：tabpfn 8.5.0（已不用）、shap 0.51.0（未用于 R2 正文）。
  - 无 conda；用 `.venv/bin/python` 跑脚本。
- ⚠️ 脚本里的 Mac 绝对路径已统一改为 `_ROOT` 自动推导（可移植）。跑训练用 `DEVICE=cuda`。

## 三、论文当前状态（paper/paper_draft.md）

- 主线 R2，章节：1 引言 / 2 相关工作 / 3 方法 / 4 实验(4.1-4.5) / 5 讨论 / 6 结论；图 1-6、表 1-3、参考文献 16 篇（按首次出现顺序编号）。
- 已完成的重大修改（本会话）：
  1. 主模型从 Bi-LSTM 拼接 → Token-Attn；数字从 0.748 → 0.918。
  2. 去 TabPFN/R3、"断点续训 bug"失败过程、"双轨"措辞 → 改"多视角"。
  3. 审稿意见（update3）12 条已基本处理完（显著性限定、文章号 67703、电池类型 5 类、温度矛盾、孤儿引用、Top6→5、Recall 说明、Model Card 口径、2.6 倍歧义、§4.5 合并、引用编号顺序）。
  4. 新增 §4.5 异常成因机制分析（分型 + 终止方式对比 + 时序因果链 + 温度因果角色 + 机制-特征-运维映射）。
- 图文件：paper/figures/ 下 fig1_fault_fingerprint、fig2_roc_pr、fig3_attn_feat_seg、fig4_attn_shift、fig5(案例 case_*.png)、fig6(mechanism_subtypes_causal)。**孤儿文件 `fig5_feat_importance_alignment.png` 已不被引用，可删。**

## 四、数据

- 主数据 `data/real/fusion_data.pkl`：X_tensor(6 通道时序, padding 200) + X_feat(62 维) + feat_cols + y + tx；train 13505(642 故障)/val 3377/test 2776(129 故障)。
- 划分：owner1-6 训练(内部 80/20 切 val)、owner7-8 测试（跨站点，口径不可变）。
- `data/real/` 已通过 .gitignore 白名单进 git（`!data/real/*.parquet` `!data/real/*.pkl`）。
- 62 维特征分组（5 类）：基础统计量 32 / 温度变化 6 / 分段端点差分 15 / 超温标志 4 / 电池类型 5。

## 五、关键结果文件（docs/，均已落盘）

| 内容 | 文件 |
|------|------|
| Token-Attn 7 seed 结果/概率/权重 | `c1c2_tokenattn[_s{seed}]_results.json` / `_prob.npy` / `_model.pt` |
| 集成 + 统计检验 | `p0a_seed_ensemble.json`、`p0b_stat_test.json` |
| 特征验证(P0c) / TabPFN(P0d) / 消融(P0e) | `p0c_feature_validation.json`、`p0d_tabpfn*.json`、`p0e_feature_ablation.json` |
| 置换特征重要性 | `r3_feature_importance.json` + `r3_figs/` |
| 机制分析 + 深化 + 因果链 | `r2_mechanism.json`、`r2_mechanism_deep.json`、`r2_causal_chain.json` + `r2_figs/` |
| 注意力图 | `tokenattn_attn_figs/`（4 张） |
| Mac 原始基线备份 | `_mac_baseline_backup/` |
| 本会话各阶段汇报 | `汇报_2026-09-02_2~9*.md`、`审计报告_2026-09-02.md`、根目录 `AUTORUN_实验报告.md` |

## 六、待办（下次可做）

1. **英文版 TAIG 投稿翻译**（当前论文为中文，尚未翻译）。
2. 可选小优化：摘要偏长、§4.3/§4.5 把"消融"叫"因果"（可改"干预"）、Hyper-Network [3] 是否移出表 1。
3. 删孤儿图 `paper/figures/fig5_feat_importance_alignment.png`。
4. 若老师要保留"SHAP"表述：可补 KernelSHAP（当前用置换重要性，更严谨）。

## 七、重要注意事项

1. **TabPFN API key**：用户曾在终端提供（tabpfn_sk_...），**切勿写入任何文件**；R2 已不用 TabPFN，该 key 无需再用。
2. **git 状态**：大量未提交改动（scripts/ 新脚本、paper/paper_draft.md、paper/figures/fig2 等、docs/ 结果 json）。用户尚未决定提交范围。
   - 明确不提交：`shell.md`、`data/real/seq_features copy.json`/`seq_features_v2 copy.json`、`docs/_mac_baseline_backup/`、5 个 `汇报_*.md` + `AUTORUN_实验报告.md`（会话工作记录）。
3. **训练脚本关键点**（复现手册详见 `docs/汇报` 与脚本 docstring）：
   - `train_c1c2.py` 主框架，`ONLY` 已是精确匹配（本会话修过子串匹配 bug）。
   - tokenattn 高 seed 方差（单 seed 0.874±0.033），务必用 7-seed 集成报告。
   - 注意力图脚本 `make_tokenattn_attn_figures.py` 已加 `torch.backends.mha.set_fastpath_enabled(False)`（torch 2.x 抓注意力必须）。
4. 数据口径（写论文/实验必一致）：62 维特征 + 6 通道，owner1-6 训练 / owner7-8 测试，主指标 PR-AUC。

## 八、一句话给下次会话

R2 论文已成型：Token-Attn（0.918 超 GBDT）+ 多视角可解释 + 成因机制分析 + EU AI Act 合规，中文稿已按审稿意见基本修完；下一步是英文翻译与投稿前小优化。
