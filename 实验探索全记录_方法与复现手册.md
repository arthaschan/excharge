# excharge 实验探索全记录：模型选型路线、方法与复现手册

> 用途：本文件沉淀本项目从「纯 Bi-LSTM 0.351」到「Token-Attn 交互融合 0.866±0.018（与 GBDT 打平）」的完整探索过程、精确数据、踩坑教训与复现命令。**接下来做更多实验时，先读本文件再动手**——尤其是「第三节 数据与评测口径」和「第六节 复现手册」，可避免重复踩坑、保证新实验口径可比。
>
> 配套文档：`实验结论汇报_给导师沟通稿.md`（对外沟通版，含给导师的三条突破口与话术）；本文件是对内的完整技术记录。

---

## 一、项目一句话与当前状态

- **任务**：充电站异常检测（二分类：正常/异常采样序列），跨站点泛化（owner1-6 训练 → owner7-8 测试）。
- **会议**：TAIG（AI 治理），卖点 = **可解释的端到端深度异常检测**。
- **导师约束**：端到端深度学习，不接受 XGBoost 当主模型（黑盒）；可解释性优先。
- **当前主模型（候选）**：**Token-Attn 多模态交互融合**（BiLSTM 分段池化 token + 62 维特征 token + [CLS] 联合 self-attention）。
  - 3-seed PR-AUC **0.866 ± 0.018**（0.8512 / 0.8864 / 0.8605），vs LightGBM 0.868 → **统计打平**。
  - 注意力可解释性图已交付（`docs/tokenattn_attn_figs/` 4 张图，详见「七 7.3」）。

---

## 二、模型旅程时间线（每个数字为什么变，务必理解）

| 阶段 | 模型/做法 | PR-AUC | 发生了什么 |
|------|----------|--------|-----------|
| 起点 | 纯 Bi-LSTM（端到端，无手工特征） | 0.351 | 导师要端到端，但原始 6 通道序列信息密度低 |
| 旧融合 | BiLSTM(max-pool)+62 特征拼接 | **0.748** | 曾以为是"深度天花板" |
| ⚠️ 认知修正 | 同上模型，**完整训练**（不续训） | **0.810** | 见「五-1」：断点续训 bug 压制了真实水平，0.748 是假象 |
| C1 强化 | bilstm（BATCH=64 重训基线）/ 多尺度池化 / Focal | 0.829 / 0.806 / 卡死 | C1 强化方向**无效** |
| C2 交互 | crossattn（特征 query 序列） | 0.845 | ⬆️ 第一种有效交互 |
| C2 交互 | **tokenattn**（多模态 Transformer） | **0.851** | ⬆️ 深度最高 |
| C2 交互 | gated（门控） | 0.796 | ⬇️ 交互方式不对反而退步 |
| seed 复现 | tokenattn × 3 seed | **0.866±0.018** | 稳定；seed123 单次 0.886 反超 LightGBM |
| 参照系 | LightGBM / XGBoost（62 特征） | 0.868 / 0.861 | GBDT 天花板 |
| 失败范式 | LSTM-AE 重建式 / CORAL 域自适应 | 0.14 / 0.825(≈基线) | 见「四」 |

**一句话总结**：深度模型不是打不过 GBDT——是「训练 bug + 简单拼接」让它看起来打不过。修好训练、换成注意力交互融合后，端到端可解释深度模型与 GBDT 持平。

---

## 三、数据与评测口径（做实验前必读，防口径坑）

### 3.1 数据
- 源：`data/real/all_data.parquet`（深圳 30 站，**1,556,000** 采样点，2020-07 ~ 2024-03）。
- 主数据文件：`data/real/fusion_data.pkl`
  - `X_tensor`：6 通道变长序列（z-score，逐序列归一化），通道顺序 = `['chargingv','charginga','out_power','charging_gun_temperature1','charging_gun_temperature2','current_soc']`。
  - `X_feat`：**62 维**手工特征（z-score，fit on train）。
  - `feat_cols`：62 个特征名（顺序固定，注意力图/消融按此索引）。
  - split：train 13,505 / val / test 2,776（Sheet7 364 + Sheet8 2,412），**跨站点**：owner1-6 → 内部 80/20 切 val；owner7-8 全做 test。seed=42 分层抽样切分，**数据划分固定**，不可变。
  - 异常率 test ≈ 4.7%？→ 随机 PR-AUC 基线 ≈ 0.047。
- `owner` 标签在 parquet 里有（Sheet1~8），fusion_data.pkl **没存**——域自适应实验需重新提取（`scripts/extract_owner.py` 已做，test 域 = Sheet7+Sheet8）。

### 3.2 62 维特征构成（口径：32+13+8+4+5 已经废弃，正确分组见下）
按代码插入顺序（= `feat_cols` 顺序）天然成块，语义分组：
1. **基础统计量 32**：v/a/p 的 mean/std/min/max/first/last/slope、soc_first/soc_last/soc_delta、t1/t2 的 mean/max/last、n_points、duration_min、total_kwh、p_v_ratio
2. **温度变化 6**：t1_slope、t2_slope、t1_max_jump、t2_max_jump、t1_std_2nd、t2_std_2nd
3. **分段/端点/差分 15**：v_first_third_min、v_sag_from_mean、a_last_third_max、a_first_last_ratio、p_max_jump、soc_rate、soc_last_rate、v/a_seg_change_*、a_seg3_max、v/a/p_last3_vs_first3
4. **超温标志 4**：t1_over_40、t2_over_40、t1_over_45、t2_over_45
5. **电池类型 5**（one-hot）：bt_LFP、bt_NMC、bt_LMO、bt_LCO、bt_LP

> ⚠️ 旧的「32 基础 + 13 一阶差分 + 8 分段/端点 + 4 超温 + 5 电池」分组是错的，已废弃。以本文件 3.2 为准。

### 3.3 评测口径（所有实验统一，才能横向比）
- **主指标：PR-AUC**（average_precision，阈值无关）——测试集 = owner7-8。
- 辅助：AUC、F1(th=0.5)、**校准 F1**（验证集上扫阈值 0.05~0.95/0.01 步长取最大 F1 的 th，再在测试集报告）。
- **seed 影响权重初始化 + batch 顺序；数据划分固定**，不同 seed 的测试集完全一致，PR-AUC 可直接比。
- ⚠️ 57 维 vs 62 维：XGBoost 用 57 维（旧特征集）= **0.893**，62 维 = **0.861**——不是"多 5 个电池列"的关系，旧特征集本身可能有实质差异。**论文/汇报一律标注特征版本**，跨版本比 PR-AUC 会出错。

---

## 四、四大范式实验记录与结论

### 4.1 范式一：监督点分类 + 深度模型（主线，成功）
见第二节旅程 + 第六节复现。结论：**融合手工特征 + 注意力交互是深度模型的正确用法**；纯序列端到端（0.351）和堆更强的序列编码器都无效。

各编码器对比（owner7-8 test，**同框架同口径**，train_c1c2.py）：

| 变体 | 类型 | PR-AUC | F1(th=0.5) | AUC | 结论 |
|------|------|--------|-----------|-----|------|
| Bi-LSTM 拼接 | 基线 | 0.829 | 0.769 | 0.967 | 参照 |
| Bi-LSTM 多尺度池化 | C1 | 0.806 | 0.739 | 0.972 | 无增益 |
| Cross-Attn（特征 query 序列） | C2 | 0.845 | 0.753 | 0.977 | ⬆️ 有效 |
| **Token-Attn（多模态 Transformer）** | C2 | **0.851** | 0.734 | 0.984 | ⬆️ **主模型** |
| Gated（门控） | C2 | 0.796 | 0.693 | 0.961 | ⬇️ 退步 |

> **注意力可解释性已落地**（`docs/tokenattn_attn_figs/`）。关键证据：异常样本中模型对**启动阶段（2/8 段）**的注意力显著下降（−0.049~−0.057），向中后段（5/8 后）漂移——可读出"前段特征塌缩"型异常模式（图 4）。最热的特征是 `a_first_last_ratio`、`a_seg3_max`、`t1_max_jump`、`v_last3_vs_first3`（图 3）。详见「七 7.3」。

早期（train_newmodels.py，早停，非断点续训 bug 期，但可对比编码器）：iTransformer 0.592（退步）、PatchTST 0.744（≈）、FT-Transformer 0.716、Dual-Transformer 0.275（崩）。**换序列编码器路已封死，不要再试。**

### 4.2 范式二：重建式异常检测（LSTM-AE）—— ❌ 失败
- 方法：只用正常样本训 LSTM-AutoEncoder，重建误差当异常分数。
- 结果：score_mean PR-AUC **0.140** / score_max **0.054**（AUC 0.853/0.574）。
- 诊断：本数据异常是「超温、SOC 跳变」这类**局部微弱偏移**，AE 把微弱异常也平滑掉了；且跨站点正常基线漂移。
- 教训：**不要在本数据集上继续投重建式**（Anomaly Transformer/USAD 大概率同样失败，机制同类）。

### 4.3 范式三：域自适应（CORAL）—— ❌ 收益≈0
- 方法：深度融合 + CORAL loss（对齐源/目标域表征协方差）。
- 结果（BN 泄露修复后）：lam=0 → 0.8245，lam=0.5 → 0.8250（**差 0.0005，纯噪声**）。
- 意外收获：这次实验顺带暴露了「断点续训 bug」（见五-1），并验证融合完整训练真实水平 0.81。
- 教训：CORAL 在这数据上没戏。若真要讲"跨站泛化"故事，需要更复杂方法（DANN 域对抗等），投入大、收益未验证——**除非导师明确要这条线，否则不碰**。

### 4.4 范式四：GBDT 家族（参照系天花板，导师已否为主模型）
- LightGBM（62 特征）：PR-AUC **0.868**，AUC 0.986，calib F1 0.726（th=0.22）；best_iter 143，训练 0.8s。
- XGBoost（62 特征）：0.861；XGBoost（57 特征旧版）：0.893。
- 用途：作为"黑盒参照天花板"。论文里深度模型与它打平 + 可解释 = 卖点。

---

## 五、重大发现与踩坑记录（每条都是学费，新实验前自查）

### 5.1 🐛 断点续训 bug（本项目最大的坑，已改写了结论）
- 现象：同样的融合模型，断点续训 0.738 / 完整训练 0.810（+0.072）。
- 根因：为规避 10 分钟超时把训练拆成"每 5 轮"分块跑，但 **只保存了模型权重，没保存 AdamW 优化器的动量/方差** → 每次重启优化器从零热身，训练轨迹被反复打断，模型学到的表征"过拟合源域"，跨站 test 崩。
- 修复：**一律完整训练（无断点续训）**；真要续训必须 `torch.save` 优化器 state_dict 一起存。
- 教训：任何"训练数字"先问一句：这是完整训练还是续训的？**不完整训练的数字不可信**。

### 5.2 🐛 BatchNorm 数据泄露（CORAL 假 0.81 事件）
- 现象：CORAL 初跑 lam=0 出 0.81 的"假突破"，val_f1 却只有 0.68（反向异常）。
- 根因：训练时对目标域（test）前向算表征，`FeatMLP` 的 `BatchNorm1d` 顺手用 test 更新了 running stats = **模型提前见过测试分布**。
- 修复：目标域前向用 `feat_enc.eval()` 冻结 BN。
- 教训：**任何在训练 loop 里对 test/目标域做前向的代码，都要检查 BN/Dropout 泄漏**。自查法：val_f1 与 test PR-AUC 走向矛盾时，优先怀疑泄露。

### 5.3 🐛 BatchNorm1d 的 batch=1 崩溃
- 现象：`BATCH=64` 时训练集 13,505 = 211×64+1，最后一批 1 个样本，`BatchNorm1d` 报 `Expected more than 1 value per channel`。
- 修复：训练循环加 `if len(idx) < 2: continue`（train_c1c2.py 已改）。
- 教训：含 BatchNorm 的脚本降 batch 前先算 `N_train % BATCH`；余 1 就跳过单样本批或换 LayerNorm。

### 5.4 💀 前台跑 MPS 长训练被 sandbox 杀（Exit 137，踩了两次）
- 现象：前台跑长训练被 SIGKILL（Exit 137），`memory_pressure` 显示内存 64% 空闲——**不是真 OOM，是前台长进程被限制**。
- 修复：一律 `run_in_background=true` 后台跑。CORAL、C1/C2 两次中招。
- 排查法：先 `memory_pressure | tail -3` 区分真 OOM vs sandbox；再查 `pgrep -lf python`。

### 5.5 💀 MPS 死锁（focal 卡死 8 小时事件）
- 现象：`bilstm_focal` 在 ep20 卡死，进程活着但 8 小时无输出，`Pageouts 140万`（swap 颠簸）。
- 处理：kill；**已放弃 focal 变体**。
- 教训：长训练务必**每个模型跑完立即落盘单独 JSON**（train_c1c2.py 已做到）；训练时注意系统 swap；若换 loss/结构后明显变慢且卡住，先降 BATCH 或放弃该变体，别干等。

### 5.6 ⚠️ 认知陷阱：早停 vs 完整训练
- train_newmodels.py（iTransformer 等）用**早停**，train_c1c2.py 用**完整训练**。跨脚本比 PR-AUC 前先确认训练协议一致。已确认：c1c2 框架内 5 个变体同协议、可直接比；iTransformer 等的退步结论不受协议影响（差太远）。

---

## 六、复现手册（照做就能跑新实验）

### 6.1 环境
```bash
PY=/Users/arthas/.workbuddy/binaries/python/envs/default/bin/python
# 依赖: torch(MPS) numpy pandas sklearn pyarrow xgboost lightgbm matplotlib
```

### 6.2 主实验脚本 `scripts/train_c1c2.py`（新增变体都加这里）
- **环境变量**：`ONLY`（跑哪个，空=全跑）、`SEED`（默认 42）、`EPOCHS`（默认 30）、`PATIENCE`（默认 999=无早停）、`BATCH`（默认 256；内存紧用 64）、`DEVICE`（默认 mps；`DEVICE=cpu` 可查 bug）。
- **输出**：`docs/c1c2_{name}[_s{SEED}]_results.json`（指标）、`docs/c1c2_{name}[_s{SEED}]_prob.npy`（测试概率）、**`docs/c1c2_{name}[_s{SEED}]_model.pt`（best checkpoint，做可解释性图/推理用）**。
- **新增模型步骤**：① FACTORIES 加 `'名字': lambda: 模型类(FEAT_DIM)`；② 模型类放 C2 区块，forward 签名 `(x, L, f)` → logits [B,2]；③ 若要可解释性，结构里带注意力模块（crossattn 的 cross-attn / tokenattn 的 encoder）。
- **固定协议（别改）**：加权 CE（pos_weight≈20）、AdamW lr 1e-3 wd 1e-4、cosine、30 epoch、clip_grad 1.0、BATCH 注意 5.3。

```bash
# 典型命令（一律后台跑！）
cd /Users/arthas/git/excharge/scripts
SEED=123 BATCH=64 ONLY=tokenattn $PY -u train_c1c2.py   # 后台 (run_in_background)
```

### 6.3 注意力可解释性图 `scripts/make_tokenattn_attn_figures.py`
- 前置：`docs/c1c2_tokenattn[_s{SEED}]_model.pt` 存在（重训 tokenattn seed123 已产出）。
- 运行：`SEED=123 $PY make_tokenattn_attn_figures.py` → `docs/tokenattn_attn_figs/` 下 4 张图：
  - fig1 [CLS] 注意力分布（序列段 vs 特征 token，正常/异常对比）
  - fig2 特征组 × 序列段热力图（异常样本）
  - fig3 62 特征 × 8 段全量热力图（异常样本，按特征组着色）
  - fig4 异常−正常注意力偏移热力图
- hook 的是**最后一层** TransformerEncoder self-attn，跨 4 头平均；8 段 = padding 后 200 步的 8 等分（相对充电进程位置，尾段可能含 padding，由 mask 处理）。
- **关键技术点（别忘了）**：PyTorch 2.x 的 `TransformerEncoderLayer` 内部调用 `self_attn` 时传 `need_weights=False`（返回 `None`），直接 `register_forward_hook` 抓不到权重。脚本采用 **monkey-patch 实例 forward** 强制 `need_weights=True, average_attn_weights=False`（返回 `[B,H,S,S]`），TransformerEncoderLayer 内部取 `out[0]` 不受影响。这是 torch 2.x 抓注意力的标准做法，新写的 hook 脚本务必照搬。
- **8 段说明**：脚本第 29 行 `K = T.TokenAttnFusion(62).K` 读 K=8；前向时把 padding 后的 200 步等分 8 段做 mean pool。变体改 K 时脚本里 `K` 不变可能错位。

### 6.4 评测工具
- `scripts/train_gbdt.py`：LightGBM/XGBoost 参照（秒级）。
- `scripts/extract_owner.py`：需要 owner 标签时重建（train 域 Sheet1-6 / test 域 Sheet7-8）。
- `scripts/eval_fusion.py` / `docs/*_prob.npy`：已有概率文件可直接重算指标（换阈值、画 PR 曲线）。

### 6.5 新实验 checklist
1. 读本文档三节（口径）+ 五节（坑）；
2. 模型加进 train_c1c2.py，先 `EPOCHS=1` 冒烟（CPU 可查 bug、MPS 验 OOM）；
3. `N_train % BATCH != 1`（或已有跳过逻辑）；
4. **后台跑**全量 30 epoch；
5. 跑完读 `docs/c1c2_{name}_results.json`，与 0.829（拼接基线）/0.851（tokenattn）/0.868（LightGBM）对比；
6. 有希望的变体 → 3 seed 复现 → 出注意力图/SHAP → 才进论文。

---

## 七、下一步候选实验方向（给"做更多实验"的弹药库）

### 7.1 立即可做、收益明确（建议优先）
| 方向 | 做法 | 预期 |
|------|------|------|
| **seed ensemble** | 现有 3-4 个 seed 的 `*_prob.npy` 概率取平均再算指标 | 通常 +0.005~0.015，方差降，论文标准做法 |
| **评测协议正式化** | 固定「PR-AUC 主指标 + 校准 F1 + 3-seed 均值±std」为论文唯一口径 | 论文严谨性必需 |
| **统计检验** | bootstrap/DeLong 检验 tokenattn vs LightGBM 的 PR-AUC 差异 | 支撑"与 GBDT 打平"（无显著差异）主张 |
| **特征组消融** | 逐一去掉 5 组特征（3.2）重训，量化各组贡献 | 双倍价值：方法证据 + 可解释性佐证 |

### 7.2 模型再探（有上限预期，别指望质变）
| 方向 | 做法 | 风险 |
|------|------|------|
| tokenattn 放大 | K=8→12/16 段、层数 2→3/4、d 64→128 | 更吃内存/时长，可能过拟合（当前已 near-GBDT） |
| 跨层注意力 | [CLS] 用 2 层注意力加权而非只取最后层 | 微增益，代码量中等 |
| 混合交互 | crossattn（特征→序列）+ tokenattn 联合 | 结构复杂，先跑 ablation 确认不是堆参 |
| 类不平衡再调 | focal 已废；可试 asymmetric CE、阈值偏置学习 | focal 卡死教训在先，谨慎 |

### 7.3 可解释性加厚（TAIG 卖点主线）
| 方向 | 做法 | 状态 |
|------|------|------|
| 注意力图 | `docs/tokenattn_attn_figs/` 4 张图（[CLS] 分布 / 特征组×段 / 62特征×8段 / 异常−正常偏移） | ✅ 已交付，详见 6.3 |
| 双轨对齐 | 特征分支用 SHAP（`explain_fusion_shap.py` 思路）+ 序列分支注意力/Grad-CAM，展示"两轨结论一致" | 待做 |
| 敏感性/反事实 | 把某特征推成极端值看预测翻转（`explain_fusion_sensitivity.py` 已有思路） | 待做 |
| 案例深描 | 挑 3-5 个 test 异常样本：曲线 + 注意力热力 + 特征贡献并排，讲"为什么判异常" | 待做 |

### 7.4 战略级（改动大，等导师方向再动）
| 方向 | 说明 |
|------|------|
| 域自适应深耕 | CORAL 无效；DANN 等更复杂，除非导师点名 |
| 站点图建模 | MTAD-GAT 类，需要构造站点关系图，叙事新但投入大 |
| 分布漂移/校准 | 模型不确定性（温度缩放）、OOD 检测——贴 AI 治理主题，可作为延伸章节而非主线 |

---

## 八、关键文件索引

| 文件 | 作用 |
|------|------|
| `实验结论汇报_给导师沟通稿.md` | 对导师沟通版（含话术），当前为"与 GBDT 打平"叙事 |
| `scripts/train_c1c2.py` | **主实验框架**（6 变体 + checkpoint 落盘 + 可解释 hook 入口） |
| `scripts/make_tokenattn_attn_figures.py` | 注意力可解释性图（本次新增） |
| `scripts/train_gbdt.py` / `train_recon.py` / `train_coral.py` | 三方向范式实验（结论均为负面/参照） |
| `scripts/build_fusion_data.py` | 62 特征定义 + 数据划分（口径源头） |
| `docs/c1c2_*_results.json` / `*_prob.npy` / `*_model.pt` | 全部结果、概率、权重 |
| `docs/c1c2_tokenattn_attn_figs/` | 注意力图输出目录 |
| `docs/fusion_v2_*_断点续训备份.*` | 旧版（断点续训 bug 产物）备份，勿再引用 0.748 |
| `实验结论汇报` 内的完整结果表 | 对外口径的最终数据 |
