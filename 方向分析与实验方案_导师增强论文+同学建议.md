# excharge 方向分析与实验方案（基于导师增强论文 + 同学建议 + H100 算力）

> 日期：2026-09-02 ｜ 作者：陈天元（内部方案）
> 输入材料：
> 1. **导师指引**：Wen et al., *Time Series Data Augmentation for Deep Learning: A Survey*（arXiv:2002.12478，PDF 在 `update2/`）
> 2. **同学 AI 分析**（4 份 docx，`update2/`）：模型瓶颈分析与突破建议 / 两步决策方案 / TabPFN 说明 / 突破建议可执行操作手册
> 3. **我方最新实测**（9/2 下午刚完成，与同学写作时点的旧状态不同，见第三节）
>
> ⚠️ 阅读前提：同学文档写于今早，当时我方结论还是"融合 0.748 打不过 XGBoost"。**今天下午情况已变**（bug 修复 + Token-Attn 3-seed 0.866±0.018 与 LightGBM 打平）——本方案先校准现状，再决定哪些建议还有效。

---

## 一、一句话结论

**同学建议的靶子已过时（XGBoost"0.894"实为 57 维旧特征口径，62 维口径真天花板是 LightGBM 0.868）；深度模型已从"0.748 打不过"变成"0.866±0.018 与 GBDT 打平"。因此方案主线不再是"追 0.894"，而是：在导师给的数据增强方向上，用 H100 算力把深度端到端（Token-Attn）从"打平"推向"超越"，同时把增强写成方法贡献。**

---

## 二、导师方向解读：这篇综述给我们的弹药

《Time Series Data Augmentation for Deep Learning: A Survey》（Wen et al., TMLR 2022）系统综述时序增强，并对**分类 / 异常检测 / 预测**三任务做了实证。与我们相关的要点：

### 2.1 论文实证结论（异常检测，最硬的一条）
Table 2（Yahoo! 数据集，U-Net 异常检测）：
| 方案 | Precision | Recall | F1 |
|------|-----------|--------|-----|
| U-Net-Raw（原始序列直接训） | 0.473 | 0.351 | 0.403 |
| U-Net-DeW（**先分解**，对残差建模） | 0.793 | 0.569 | 0.662 |
| U-Net-DeWA（**分解 + 数据增强**） | 0.859 | 0.581 | **0.693** |

→ **"先去掉确定性分量，再对残差建模，最后加增强"带来 +0.29 F1**。这直接对应我们的场景：充电曲线有强确定性形状（SOC 决定功率曲线），异常是叠加其上的"超温 / SOC 跳变 / 电压塌陷"等局部突变。**残差化/去趋势预处理可能让 6 通道原始序列对深度模型重新"有信息"**（呼应注意力图发现：异常时模型把注意力从中前段移向中后段，说明真正异常信号藏在序列局部）。

### 2.2 论文方法分类（我们可用的方法池）
- **基础变换类（时域）**：jittering（加噪）、scaling、time warping、window slicing/cropping、flipping、permutation、**mixup（样本插值）**
- **频域/时频域**：AAFT/IAAFT（幅值+相位扰动，保持自相关结构）、小波 MODWT 系数扰动
- **高级**：序列分解后增强（DeW/DeWA 类）、**深度生成模型**（GAN/VAE/GP/Flow 合成样本）、SMOTE 类时序过采样
- **组合选择**：RandAugment 式自动增强组合（论文列为未来方向）
- **与类不平衡结合**：增强只作用于少数类 + loss 加权（5.2 节明确推荐）

### 2.3 论文点到我们的痛处
> "limited labeled data calls for effective data augmentation" + 类不平衡在时序分类中常见 → **我们恰好双中**：训练仅 642 条故障序列（正类 4.7%），深度模型在类别不平衡 + 小样本上最吃亏。增强是导师给的、对症的药。

---

## 三、现状校准（同学建议哪些已过时，必须先改口径）

| 同学文档里的说法 | 我方最新实测（9/2） | 结论 |
|----------------|-------------------|------|
| 融合模型天花板 0.748 | 断点续训 bug 修复后 0.829；Token-Attn **0.866±0.018** | ❌ 已过时，作废 |
| 靶子 XGBoost（62 维）= 0.894 | **0.894 是 57 维旧特征**；62 维 XGBoost 实测 = 0.861；62 维真天花板 = **LightGBM 0.868** | ⚠️ 打错靶子，口径必须统一到 62 维 |
| 深度模型"无一接近 XGBoost" | Token-Attn 0.866 ≈ LightGBM 0.868（统计打平），单 seed 最高 0.886 反超 | ❌ 已过时 |
| 重建式 LSTM-AE（路径 C）值得做 | 已实测 **PR-AUC 0.14，失败** | ❌ 已证伪，不要再投（除非导师点名） |
| CORAL 域自适应 | 已实测收益 ≈ 0（0.8245→0.8250） | ❌ 已证伪 |
| iTransformer / PatchTST / FT / Dual 待试 | 全部实测：0.592 / 0.744 / 0.716 / 0.275 | ❌ 已试完，均无突破 |

**仍有效、值得采纳的同学建议**：
1. **特征验证实验**（二阶差分 / 变点 / catch22 / 跨通道滞后 → 固定分类器换特征）：同学唯一还没跑、且能当"分水岭"的实验。✅ 采纳，作 Phase 1 开关。
2. **TabPFN-2.5 探路**（62 维直接跑，不加融合）：低成本旁证"换分类器有没有空间"。✅ 采纳（但靶子改为 LightGBM 0.868；且 H100 才跑得动大模型推理，Mac 不合适）。
3. **路径 B 自监督预训练**（TS2Vec / masked 重建，全量无标签序列 → 微调）：✅ 采纳，**这是 H100 迁移后性价比最高的深度主攻方向**（Mac MPS 上做 15k×200×6 的对比预训练不现实）。
4. 数据增强是"辅助、治标不治本"——❌ **这个判断作废**：在导师把增强提为方向、且我们已有 0.866 打平基线的今天，增强从"辅助"升级为**主线方法贡献**。
5. 路径 D（换问题定义：实时中断/早检）：保留为备胎，不优先。

---

## 四、推荐方案（决策树 + 实验矩阵）

### 4.1 决策逻辑（融合同学"两把尺子" + 增强主线）

```
Phase 0（H100 到手，几小时，纯计算）：
  ├─ 3-seed 概率集成 + DeLong/bootstrap 检验   → 坐实 "0.866 与 LightGBM 0.868 打平"
  ├─ 特征验证实验（开关）：62维+新特征 → XGBoost
  │    ├─ 若 PR-AUC 明显涨（>0.90）→ 时序里还有货 → Phase 2 预训练必须上
  │    └─ 若 ≈0.868 → 信号已被统计量榨干 → Phase 2 侧重"增强正则 + 残差化"而非挖新信息
  └─ TabPFN-2.5 on 62 维（旁证）：
       ├─ 若 >0.868 → "最新模型也印证天花板"，增强/预训练超越才有意义
       └─ 若 ≤0.868 → 换分类器无空间，死磕深度 = 唯一路

Phase 1（1-2 天）：数据增强主线（导师方向）
  ├─ 在 train_c1c2.py 给 Token-Attn / Bi-LSTM 拼接加"正类条件增强"：
  │    jittering / scaling / time warp / window slice / 频域扰动 / 时序 mixup（只增强 642 正类）
  ├─ 目标：PR-AUC 0.866 → 0.88+（单跑多 seed，挑稳的）
  └─ 论文卖点 = "小样本 + 强类不平衡下的时序条件增强"

Phase 2（H100 解锁，2-4 天）：残差化 + 自监督预训练
  ├─ 预处理：充电曲线分解（去 SOC 确定性分量）→ 残差通道进序列分支
  │    （复刻论文 U-Net-DeW 的 +0.26 逻辑）
  ├─ 预训练：TS2Vec / masked 重建，全量序列（含 owner7-8 无标签）→ 冻结/微调接入融合头
  └─ 评估是否 0.88 → 0.90 区（若特征验证证明还有货，预期最高）

Phase 3（可选，讲故事）：若增强+预训练后仍卡 0.87 → 与导师对齐
  ├─ 路线 1：承认 62 维口径下深度 ≈ GBDT（打平 + 注意力可解释性已够写 TAIG）
  └─ 路线 2：路径 D（实时中断/早检）换问题定义，重塑时序主场
```

### 4.2 实验矩阵（优先级排序）

| # | 实验 | 回答什么 | 成本(H100) | 成功判据 | 状态 |
|---|------|---------|-----------|---------|------|
| P0a | 3-seed 概率集成 | 能否白拿 +0.005~0.015 | 分钟级 | PR-AUC > 0.866 | 待跑 |
| P0b | DeLong/bootstrap 检验 tokenattn vs lightgbm | "打平"是否统计成立 | 分钟级 | p>0.05（无显著差） | 待跑 |
| P0c | **特征验证**（4 类新特征→XGBoost 62维） | 时序里还有没有货 | ~半天 | >0.90 有货 / ≈0.868 榨干 | 待跑（同学方案，采纳） |
| P0d | TabPFN-2.5 on 62 维 | 换分类器有无空间 | 1-2h | vs LightGBM 0.868 | 待跑（同学方案，采纳） |
| P1a | 正类条件增强（jitter/scaling/warp/mixup）→ Token-Attn | 增强能否把深度推过 0.87 | 每个变体 ~20min×N | 单变体 3-seed 均值 > 0.866 | 待跑（导师方向主菜） |
| P1b | 分解残差化预处理（STL/SOC 拟合去趋势）→ 序列分支 | 论文 U-Net-DeW 逻辑能否复现 | 半天 | 残差通道输入不降且更稳 | 待跑 |
| P2a | 自监督预训练（TS2Vec/masked）→ 融合头微调 | 深度模型数据效率优势能否兑现 | 1-2 天 | 端到端 PR-AUC > 0.87 | 待跑（H100 解锁） |
| P2b | 增强 + 预训练组合 | 叠加效应 | 1-2 天 | 逼近/超过 0.88 | 待跑 |
| — | 重建式 / CORAL / 换编码器 | （同学建议中的死路） | — | — | **已实测证伪，不再跑** |

### 4.3 对论文的叙事升级（给 TAIG）
- 旧叙事（同学的路径 E）："打不过就认 GBDT SOTA，把卖点转合规" —— 已被 0.866 打平推翻。
- **新叙事**："端到端可解释深度模型与 GBDT 打平（3-seed 0.866±0.018，注意力图佐证）+ **针对小样本强不平衡时序的增强方法学**（正类条件增强/残差化/预训练），将深度模型推过 GBDT"。
- 数据增强综述（导师给）可作为方法章节的引用基石：我们的增强组合属于综述 taxonomy 的"变换类 + 不平衡类结合"未充分探索区（论文 5.2 明说这是 future direction）——**正好填空**。

---

## 五、给导师的一句话口径（建议沟通时用）

> "我们按您给的时序增强综述方向做了梳理：瓶颈已从'0.74 打不过'修正为'0.866 与 GBDT 打平'。下一步用数据增强（正类条件增强 + 残差化 + 自监督预训练）把深度模型往上推，同时用特征验证实验判断时序里是否还有未挖掘的信号。整体计算量需要在 H100 上跑。"

---
